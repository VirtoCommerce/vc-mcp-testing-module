# Repository Findings Backlog

Incidental repository / infrastructure defects found **while doing something else**. They land here instead of being fixed inline, so a task never stalls behind them. Work them separately, in parallel, at whatever priority the team wants.

**What belongs here:** manifest defects, duplicate or dangling ids, corrupt CSV rows, stale counters, misconfigured MCP servers, broken selections, stale lint baselines — anything that is a repo-health problem rather than a product defect.

**What does NOT belong here:** product bugs (→ `reports/bugs/`), test-case quality findings (→ `/qa-review-tests`), or a defect that **blocks the task in hand** — fix that one immediately and say so.

**Convention:** newest batch on top. Keep entries one-liners with enough detail to act without re-investigating. Move a row to Resolved with the date and how it was fixed; do not delete history.

---

## Open

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-48 | **Unit-test hermeticity against `test-data/aliases.<env>.json` depends on a guard inside the production code, not on the tests being sandboxed.** Found 2026-09-15 while running `npm run test:quality`, which replaces an imported function's body with `return undefined;`. Gutting a guard in the seed-common alias-writeback path caused a unit test to write mock ids into the **tracked** `test-data/aliases.vcst.json` — `ACME_ADMIN.platform_id` and `SUPPORT_AGENT.userId` both became `"user-1"`. Normal runs are hermetic (re-verified: `user-provision`, `seed-users` and `loyalty-missions-overlay-shadow` leave the tree clean), so this is latent, not active. | The failure mode is the committed-runtime-GUID class the repo already guards (`DV-021`), arriving from the opposite direction: instead of a seeder leaking a real GUID into a committed file, a *unit test* leaks a **mock** id into one. It is silent — the suite stays green — and a commit made without checking `git status` would ship it, after which every `@td(ACME_ADMIN.platform_id)` on vcst resolves to `user-1`. | Make the unit tests structurally unable to write there rather than relying on a guard holding: point `writeEnvAliasOverride`/`syncEnvAliases` at a temp dir in unit-test context (the `_test-helpers.mjs` `withTempDir` pattern already exists), or have them refuse a write when `NODE_ENV=test`/no `TEST_ENV` is set. Cheap interim: a `td:validate` check that fails on an alias id matching an obvious mock shape (`user-1`, `acct-*`, `contact-*`). |
| B-47 | **The `catalog-edge` fixture has numeric fields no guard checks — found by the new `td:mutation-check` harness, 2026-09-15.** Perturbing `catalog-edge-specs.mjs` one edit at a time and running the unit test and `td:validate:catalog-edge` against each: 8 mutations produced 5 `BOTH` (unit test duplicates the guard), 1 `UNIT ONLY`, and **2 `NEITHER`** — two numeric literals (L76, L87) that neither the unit test nor the drift guard detects when changed. | A fixture value nobody gates is the GOLDEN RULE failure with the slow fuse: it can be edited, or drift through a re-seed, and every gate stays green while the case it underwrites quietly stops discriminating. This is the same class as B-42 (fixtures are self-certified) but located precisely rather than argued. | Run `npm run td:mutation-check -- catalog-edge` to see the two lines, then add the missing assertions to `validate-catalog-edge-data.mjs` (the guard, not the unit test — see `test-data-authoring.md` §7a). Then sweep the other 19 resolvable domains with `--all`; every `NEITHER` row is one of these. |
| B-46 | **`td:validate:missions-e2e` is RED on a clean checkout from tracked state, and had never run in CI.** `MSN_E2E_ORDERCOUNT` rewards 501 PTS and `MSN_E2E_ORDERVALUE_008` rewards 508 PTS, and each ties with a leftover `AGENT-TEST-MSN-E2E-2026-09-10-*` mission in the tracked `test-data/aliases.vcst.json` that the seed itself MEASURED as co-granting on the same order (`co_completing_missions`). Found 2026-09-15 while wiring the 22 per-domain guards into `gates.yml` — until then they ran in no workflow at all, so this had been red without a signal. | Two identical grants settling together are indistinguishable in a balance delta and in a points-history amount, so `MSN-E2E-001` and `MSN-E2E-008` cannot show that THIS mission paid out — they pass vacuously. The guard is correct and was simply unheard; it is currently listed in `KNOWN_RED` in `scripts/maintenance/td-validate-all.mjs` so the new CI gate can be switched on without masking it. | Fixture-owner decision: give each of the two missions a reward value no co-granting mission carries (the collision is with a fixture another suite owns, which the in-set divergence rule cannot see), re-seed, then delete the `KNOWN_RED` entry so the guard starts gating. |
| B-45 | **MEASURED 2026-09-11: 472 of 4,595 cases across 39 of 140 suites are affected — SIGNIN_AS 437, CLEAR_SESSION 224, RESET_CART 206, SIGNOUT 2.** Every one is `Automated` or `Semi-Automated`, declares the primitive in `Preconditions`, and has no `[PRE:*]` in `Steps` — so on the machine lane it runs with whatever session and cart happen to exist. Worst suites: 083c (42), 089 (31), 082 (30), 036 (26), 040a (26), 043 (23), 093 (21), 007 (18). Detection is now automated as **PRE-002** in `lint-test-cases.ts` (Medium, so it reports without blocking the 0-Blocker/0-Critical gate). Verified non-over-firing against a raw row: `083c MSNF-001`, `Automated`, declares `[PRE:CLEAR_SESSION]` + `[PRE:SIGNIN_AS:USER_DEFAULT]`, `Steps` contains no `[PRE:` at all. · **`[PRE:*]` preconditions are DECLARATIVE ONLY — no runner executes them — and the one collision they cannot cover is guarded by a script that 1 of 140 suites references.** Two halves. **(a)** `parsePreconditions()` in `scripts/lib/ui-step-parser.ts` parses `[PRE:RESET_CART]` / `[PRE:CLEAR_SESSION]` / `[PRE:SIGNIN_AS]` correctly, but a corpus-wide grep finds its **only callers are its own unit tests** — `classifyCase` deliberately reads `Steps` + `Assertions` and **not** `Preconditions` (stated in the file's own header). So a case carrying `[PRE:RESET_CART]` is isolated only when a *human or agent* reads the cell and acts; on the machine lane the tag is inert. Same shape as the recorded `[AUTH role=]`-in-Preconditions trap, on a different primitive. **(b)** The collision that matters most is **competing carts** — more than one cart for one `(customerId, storeId, currency)`, where xAPI resolves reads and writes to *different* carts. `scripts/seed-data/carts/check-competing-carts.mjs` exists for exactly this and says in its own header that *"`[PRE:RESET_CART]` cannot cover this: it empties the RENDERED cart through the UI, so a competing cart is invisible to it."* Measured 2026-09-11: of **140 suites**, exactly **one** (`050b1`) mentions the guard | A case can declare `[PRE:RESET_CART]`, pass every lint, look isolated in review, and run against a cart it never reset. For any case asserting a **balance or total DELTA** the failure is silent and directional: the delta measures the wrong cart and reads as a product defect at triage. This is the same class that produced two false FAILs in `REG-2026-09-11-1510` (`LOY-038`, `MSN-027` — see B-44), and it is why a `/qa-test` run spent time this session chasing a cart-identity ghost that turned out not to exist | Two independent fixes. **(1)** Either make a runner consume `parsePreconditions()` and execute the primitives, or — cheaper and more honest — have `lint-test-cases.ts` warn when a case carries `[PRE:*]` **and** is `Automated`, since that combination claims an isolation the machine lane does not deliver. **(2)** Promote `npm run carts:check` from an ad-hoc script to a declared suite-level preflight for every suite whose cases assert a cart/balance delta — the manifest already has a `notes` field and `075f`/`083e` now carry it (added this run), but 137 other suites do not and nothing enforces it. A `requiresPreflight: ["carts:check"]` manifest key that `regression:plan` honours would make it a gate rather than a habit |
| B-44 | **INDEPENDENTLY CONFIRMED the same day by an unrelated regression run.** `REG-2026-09-11-1510` (the VCST-5024 C1b exact-set run, Customer mode, touching none of the new fixtures) produced **2 of its 6 executed cases failing on exactly this class**: `LOY-038` (075c) asserted `items.0.amount > 1000` and got a 506-PTS `Mission` earn, with several other Mission earns on the same account in the same second; `MSN-027` (075d) found `currentValue = 2` and `status = Completed` after its *first* order, because the "isolated" account already carried prior progress. Root cause in both: suites `075c` and `075d` contend for one `ORG_USER`/mission account's loyalty ledger inside a single run window, so results are order-dependent. Neither is a product defect and neither was caused by that run. So the isolation gap is **not specific to the new org-loyalty fixtures** — it is already costing false FAILs in the existing loyalty corpus, where it reads as product breakage at triage. · **The repo already solved per-lane fixture isolation and the new loyalty fixtures did not inherit it — and for an org-pooled balance the split has to be per ORGANIZATION, not per account.** The established pattern is `MULTI_ORG_TF_BR` (backend lane) / `MULTI_ORG_TF_BR_ALT` (frontend lane), reserved in each alias's `_notes` and enforced **executably** by `LANES` in `scripts/seed-data/b2b/membership-lock-specs.mjs` plus `npm run td:validate:membership-lock`, which cross-checks the code against the prose. The `ORG_LOY_*` set seeded for VCST-5024 (2026-09-11) has **no lane split at all**: one organization, one set of members, consumed concurrently by the backend and frontend lanes with isolation provided only by a hand-written instruction in the dispatch brief that the frontend lane alone may write. **The fixture-isolation rule says "or get their own accounts", and separate accounts are NOT sufficient once a balance is pooled across an organization.** `.claude/rules/test-data.md` §DISPOSABLE FIXTURES rule 4 — *"two suites consuming one disposable fixture set are serialised with a re-seed between them, or get their own accounts"* — predates organization-scoped loyalty (`vc-module-loyalty` PR #17, VCST-5024). Under `Loyalty.LoyaltyBalanceCalculationMode = Organization`, an order placed by **any** member moves the **same** balance, so two agents holding entirely distinct accounts in one organization collide exactly as hard as two agents sharing one account. **The unit of isolation is the ORGANIZATION, not the account.** Observed 2026-09-11: the `/qa-test VCST-5024` backend and frontend lanes ran concurrently against one organization, isolated only by an instruction that the frontend lane alone could write. It held — the two lanes' independent readings reconcile exactly where they overlap (both 133 086; both 39 533 / 100 033 for the stranded user-scoped figures), and the one divergence is a timestamp, not interference — but it held by convention and careful agents, not by mechanism | A shared pooled balance makes every reader time-sensitive and every writer global. Had the second lane placed one order, **both** lanes' relative-delta assertions would have silently become wrong, and the designed divergence (30 000 / 90 000 / pooled 120 000) would have collapsed into a figure that cannot distinguish *pooling* from *double-counting* — the single distinction the ticket exists to test. The failure would not have errored; it would have produced confident, wrong numbers on both sides. The rule as written would have been followed to the letter and still permitted it | Amend rule 4 to name the **scope of the shared mutable state**, not the account: two lanes need their own *organization* when the fixture set is org-scoped, their own *account* when it is user-scoped. Concretely for loyalty: seed a second AGENT-TEST organization with its own two members so a backend lane never reads a pool a frontend lane is mutating — one extra seed, and it removes the coupling entirely. Separately, `/qa-test` Step 4 has no synchronisation primitive: a lane told to assert "after the other lane's orders land" is given no signal for when that is, and currently infers it. Either give the orchestrator an explicit join before dependent assertions, or forbid cross-lane dependencies and let the orchestrator do the dependent reads itself |
| B-43 | **`npm run verify:gate` silently runs none of its deterministic core on Windows — every sub-command reports `exit null` with empty captured output, and the sheet still prints as though the gate ran.** Observed twice on 2026-09-11 during `/qa-test VCST-5024`, independently by the orchestrator at `--gate 3-exec` (`td:validate` and `tc:scope` both `exit null`, both with an empty `|` output line) and by the fresh `qa-lead` verifier at `--gate 3 --suite <csv>` on **both** new suites (all three sub-commands, same signature). Both parties fell back to invoking `lint-test-cases.ts`, `validate-td-refs.ts` and `scope-existing-coverage.ts` directly, so neither gate was actually decided on a blank — but only because both happened to notice. Note `scripts/regression/verify-gate.ts` is currently modified in the working tree, so the regression may be local and recent | **This is the `SILENT` archetype pointed at the gate layer itself.** The sheet's own header says *"This sheet is EVIDENCE, not a verdict"*, and `exit null` is easy to read as "nothing to report" rather than "nothing ran" — the distinction the whole repo cares about (`.claude/skills/qa-test/SKILL.md` §Silence is never an answer). A verifier that trusts the sheet APPROVEs a hard-STOP gate on zero deterministic evidence, and the run looks fully gated afterwards. It affects `3-exec`, `3`, `5b` and `5e` — i.e. both hard STOPs | Make an unrunnable sub-command a **hard error**, never a row: if `spawn` returns no exit code, print `DID NOT RUN` and exit non-zero so the gate cannot be read as satisfied. Then fix the spawn itself — the likely cause is invoking an `npm`/`npx` shim without `shell: true` on win32, which resolves to no executable and yields a null code rather than throwing. Worth a smoke test that asserts each gate's core actually produced output, since the failure mode is precisely that it produces none |
| B-42 | **Seeded test data is self-certified: the same agent designs the fixtures, authors the seeder, authors its own drift-guard validator, runs the seed, and reports that its own validator passed.** No independent party checks that the data is *discriminating* — the `/qa-test` `3a` gate asks only for "seeded env, green `td:validate`", and the `3-exec` clause the orchestrator owns asks only whether the aliases **resolve right now**, which is a different question. The `3-cases` verifier re-derives the `data_surface: false` SKIP but rules on nothing when it was true. Measured 2026-09-11 on VCST-5024: the fixture's stated divergence plan was "A earns 30 000, B earns 90 000, pooled 120 000 — all distinct". The live deltas were **39 533** and **100 033**, because store-wide missions granted on the same orders. Divergence survived by luck; the asserted arithmetic was wrong, `td:validate` was green throughout, and the seeder's own validator could not catch it because it validates the *intent* rather than what the environment actually produces. The only check that caught it was accidental — a different agent's live readings contradicting the plan | A fixture can satisfy every `@td()`, every drift guard and every secret-hygiene check and still make the feature's central question undecidable — which is exactly what `.claude/rules/test-data.md` §SECOND RULE exists to prevent, and the rule currently has **no enforcing party**. The failure is silent and reads as rigour: a green `td:validate` plus a confident seeding report is easy to take as verified data. Note the seeder here was unusually good — it self-reported five FIXTURE-GAPs and its own falsifiability proof — so this is a structural gap, not a performance one; a less scrupulous run produces the same green output with none of the caveats | Give the `3-exec` gate a second clause the orchestrator must rule on: **re-derive the fixture's discriminating property from the LIVE environment, not from the seeder's report** — for each link under test, does the seeded data actually make a wrong implementation fail? One cheap generic form: have the orchestrator read back the values the plan claims are divergent and assert they still differ *after* seeding, since accrual side effects (missions, promotions, gift lines) routinely move them. Alternatively route the check to the specialist who will consume the data, who is independent of the seeder by construction and is already dispatched. Whichever way, the rule is the same one the verifier already follows elsewhere: never APPROVE on the doer's own summary |
| B-41 | **Two seeders read every loyalty balance as `0` on the current build, silently, because the module moved the route and they tolerate a 404.** `vc-module-loyalty` PR #17 (deployed 2026-09-11 as `3.1008.0-pr-17-116e`) replaced `GET /api/loyalty-program-operation-log/balance/{userId}` with `/balance/user/{userId}` and added `/balance/organization/{organizationId}` — confirmed against the module's own swagger and by live probe. `scripts/seed-data/loyalty/seed-loyalty-balance.mjs:144` and `scripts/seed-data/loyalty/seed-missions-e2e.mjs:624` still call the **legacy** path with `expectStatus: [200, 404]`, so the 404 is swallowed and the reading becomes `0` for every account, every time | A funding step reports "balance 0 → 0" and looks like it ran; a delta assertion built on it compares **two zeros and passes**. This is the `SILENT` archetype on the seeding layer — the tolerated 404 converts a hard failure into a confident wrong number, and it will mislead any loyalty run on this build until the route is fixed. Found 2026-09-11 while seeding VCST-5024's fixtures | Point both call sites at the new route. A shared `readLoyaltyBalance()` that probes the live routes, falls back to the operation log and **reports which source answered** already exists at `scripts/seed-data/loyalty/loyalty-earn.mjs` (added in the same run) — reuse it rather than re-fixing the URL twice. Separately, `expectStatus: [200, 404]` on a **read whose value is then used** is the wrong contract anywhere: a 404 there should fail loudly, not resolve to zero |
| B-39 | **Nothing validates the GraphQL embedded INSIDE suite CSVs against the live schema, so a breaking xAPI change disarms regression cases with every gate green.** `graphql:fixtures:validate:refresh` validates only `test-data/graphql/**` — it returned **75 passed / 0 failed** on 2026-09-11 while, on the same corpus and the same schema, **26 inline invocations across 8 cases in 3 suites** were already invalid. Measured on `/qa-test VCST-5024`: `vc-module-loyalty` PR #17 (deployed as `3.1008.0-pr-17-116e`) made `storeId` a **required** `String!` on `loyaltyBalance` and `loyaltyPointsHistory`; every corpus caller omits it — `075b` MCO-GQL-004, `075c` LOY-038, `075d` MSN-019/020/021/027/032/033. Live-confirmed by direct probe: `Argument 'storeId' of type 'String!' is required for field 'loyaltyPointsHistory' but not provided` (`PROVIDED_NON_NULL_ARGUMENTS`). `suites:lint` (138 suites OK), `lint-test-cases.ts` (all 11 static dims) and `td:validate` all pass these rows — none of them parses a query or knows a schema exists | A breaking contract change silently converts executing coverage into rows that cannot run, and **the failure surfaces in the direction that costs the most**: GraphQL returns `errors[]` inside a **200**, so a row whose assertion is prose-shaped can pass *vacuously* (the `T-005` failure mode already recorded in memory as `reference_runner_t005_prose_makes_assertions_vacuous`), and a row that does fail reads at triage like a product defect rather than a stale case. 3 of the 8 here are `Automated`, i.e. they run unattended. This was caught only because the run's `contract_surface` axis happened to refresh the schema and a human diffed the signatures — there is no path by which a gate would have reported it | Extract the inline operation text from suite rows (the runner's own `[GQL-OP]` grammar already delimits it) and validate it against the refreshed `.claude/knowledge/api/graphql-schema.md` snapshot — the same snapshot `2d` already produces — then fail it as a new `DV-*` rule in `lint-test-cases.ts` so it runs in `suites:review` and in CI. The fixture validator is the natural home for the parsing half; what is missing is only that it is pointed at one directory instead of at the corpus. Interim: the 26 invocations were repaired in-run to `storeId: "{{STORE_ID}}"` (the corpus's established token, 1,367 prior uses) and re-linted, so the immediate breakage is closed — but nothing prevents the next required-argument addition from doing this again |
| B-40 | **`tc:scaffold --check` reports a title collision caused by its own truncation without saying truncation caused it.** A sweep row is titled `${sweep.surface} — ${row.scenario}` and then passed through `truncate()`; a long `surface` string pushes the distinguishing `scenario` suffix past the limit, so N sweep rows collapse to one title and the gate fails with `Title+Section duplicates an earlier row in this batch`. Hit on 2026-09-11 with `surface: "Loyalty.LoyaltyBalanceCalculationMode Customer vs Organization across every read and spend site"`; shortening it to `"Points calculation mode"` fixed it with no other change. Separately and minor: `suites:review` takes a **path** (`--suite regression/suites/.../075d-….csv`) while `tc:scope` takes **ids** (`--suite 075d`), and passing an id to the former dies with a raw `ENOENT` stack | The message points at the wrong thing — it reads as "you wrote two cases with the same name", so the author hunts through their own `cases[]` (which are fine) rather than the `sweeps[]` block. Cheap to hit and cheap to misdiagnose, and the gate is otherwise doing exactly its job | Truncate only the `surface` half and keep the `scenario` suffix whole, or detect that two titles differ pre-truncation and say so: `sweep "<kind>" surface string is too long — N rows truncate to the same title`. For the second: accept a bare suite id in `lint-test-cases.ts --suite` by resolving it through `config/test-suites.json`, the way `tc:scope` already does |
| B-38 | **`/qa-deploy-pr`'s ticket→PR resolution is dead on this Jira instance, and it fails SILENTLY — it reports "no artifacts resolved" for a ticket that has linked PRs.** `scripts/deploy/deploy-pr-artifact.ts:239` queries dev-status with `applicationType=GitHub`, but virtocommerce.atlassian.net registers the GitHub app under the instance-type key `oAuth-com.github.integration.production`; the documented value returns `{"errors":[],"detail":[]}` with **HTTP 200**. Line 244 swallows that in a bare `catch`, so an empty result is indistinguishable from a ticket with no dev links. Measured 2026-09-11 on VCST-5024: the resolver found **0** PRs while `/rest/dev-status/1.0/issue/summary` reported `pullrequest.overall.count = 3` (vc-module-loyalty#17, vc-frontend#2475, vc-deploy-dev#6491). Probed `GitHub` / `github` / `GITHUB` / `gitHub` / `githube` / `GitHubEnterprise` — all empty; omitting the param 500s. | The documented `npm run deploy:pr -- <ticket-key>` path never resolves an artifact for **any** ticket on this instance, so every run must pass `--pr` by hand — and an operator who trusts the output concludes the ticket has no PRs and stops. Same defect shape is likely in `.claude/skills/qa-local-env/resolve-task.mjs`, which this was ported from. | Read the instance-type keys from `/rest/dev-status/1.0/issue/summary` → `summary.pullrequest.byInstanceType` and iterate them as `applicationType` (keeping the `GitHub` literal as a fallback). Separately: make the `catch` at :244 distinguish a transport failure from an empty result, and warn when `summary` reports a PR count the detail scan did not reach. |
| B-32 | **Nothing derives the focus ring's contrast from the preset source, so a palette edit can drop a gated preset below 3:1 silently.** Found on `/qa-test VCST-5653` (2026-09-10): the entire finding — Coffee light measuring **2.97:1** on header-top, footer-top and mobile-menu (all `#3d2b24`) — was reachable by arithmetic from `client-app/assets/presets/*.json` **before any browser opened**. 88 cells (7 presets × light/dark × 7 region surfaces) were computed at ref `8e45ee74`; 7 fail, 3 of them on a WCAG-gated preset. Cross-checked three ways and agreeing to 2 dp: a standalone implementation, the repo's own audited `COLOR_MATH_JS`, and live measurement with the store switched to Coffee | The one number that decides a WCAG 1.4.11 verdict for every preset is currently discovered by a human switching a shared store setting and tabbing around. It shipped once already — the PR declared the gap as "tracked separately" and no ticket existed (now **VCST-5946**). A preset recolour by a tenant or a designer can reintroduce it with nothing red anywhere. Also note the ring is **tenant-overridable** via `color_vc_focus_ring` with no contrast validation at all, so a client can reinstate the original 1.63:1 bug through config | Extend `scripts/maintenance/sync-design-tokens.mjs` (`tokens:sync`) to carry each preset's `color_accent_500/600` plus the region backgrounds (`color_body_bg`, `color_header_top_bg`, `color_header_bottom_bg`, `color_footer_top_bg`, `color_footer_bottom_bg`, `color_mobile_menu_bg`, `color_additional_50`, **and `color_primary_50`** — see below), then assert ≥3:1 for the gated presets in a unit test and let `tokens:check` gate it. That generator is the established home: its own header documents the hardcoded-spacing-grid incident that created it. **Add `color_primary_50` because it is an 8th surface no matrix covers** — the skip-link bar is tab stop #1 on *every* page and measures 4.02 / 4.47 / **3.30** (Coffee light / Coffee dark / Red light); Red light clears the floor by 0.30, the thinnest margin found anywhere |
| B-33 | **`tc:scope`'s `--domain` axis under-selects any change that has no domain.** A UI-kit / token-layer change touches every domain and belongs to none, so the scan's own vocabulary misses most of the corpus. Measured 2026-09-10 on VCST-5653: the domain-scoped invocation (`--domain cross-cutting,branding` + 3 oracles + 6 observables) returned **26 at-risk rows across 18 suites**, while a corpus-wide grep for the same concepts returned **62 rows across 13 suites** — missing b2b, bopis, configurable-products, customer-reviews, loyalty, sales-rep, search and notifications entirely | Step 2a exists to answer *which existing rows does this change make wrong*. For a shared-primitive change it currently answers that question over a third of the relevant corpus and reports a clean, confident number for it. The failure is silent and reads as coverage: nothing in the output says "the axis you scoped by does not apply to this change" | Either teach `tc:scope` a `--surface`/`--all-domains` mode for changes whose blast radius is the whole storefront, or have it warn when the changed paths are `ui-kit/**` / token files while `--domain` is set. Interim mitigation is already in place — `commands/qa-test.md` §Routing now tells a UI-kit run to pair the scan with a corpus-wide grep, and it is recorded in memory — but that is a human remembering, not a gate |
| B-34 | **Focus indicators have no executing regression protection at all, and the file that was supposed to gate them has been entirely `GAP` for seven weeks.** `critical-ui-scope.md`'s coverage matrix has had **197 `GAP` cells since 2026-07-25**, when suite `048b` — its sole carrier — was removed; `scope:validate` reports GAPs as warnings, not failures. Separately, of the **62 corpus rows that assert a focus indicator, almost every one is `Manual · NOT_EXECUTING`** (run-fate among the at-risk subset: 9 WILL_RUN vs 17 NOT_EXECUTING, 26 never audited). The three rows that would have caught the VCST-5653 failure — `045` A11Y-KB-003 / A11Y-CC-002 and `070` FWL-046 — are all Manual and have never run | This is why a gated-preset WCAG failure reached a Ready-for-test ticket with nobody noticing: there was no mechanism that could have noticed. The matrix looks like a gate and is one `--strict` flag away from being one; the corpus looks like 62 rows of coverage and executes almost none of it. Both failure modes read as coverage from the outside | Two independent fixes. **(1)** Add a focus-indicator row per component to `critical-ui-scope.md` when a replacement suite lands, and turn on `--strict` so GAPs are fatal again. **(2)** Promote the highest-value focus rows to `Automated` via `/qa-test-lifecycle` — note the VCST-5653 run authored none by operator instruction, so this is unchanged by it. B-32's drift guard is the cheaper half of the same protection and does not need a browser |
| B-35 | **Four UI-kit code paths ship with no environment able to exercise them.** Found across the VCST-5653 lanes (2026-09-10): (a) the `VcRadioButton` **`no-indicator`** variant — the WCAG 2.4.7 case, whose ring lands on a `display:none` span — has one storefront consumer (`select-address-map-list.vue`, `:no-indicator="!selectable"`) and **all 50 BOPIS pick points on vcst-qa are `selectable`**; (b) `VcNavButton`'s PDP arrows unlock only at **≥5 images** and an xAPI scan of 3000/4549 products found the distribution `{0:1, 1:355, 2:2641, 4:2, 5:1}` — **exactly one** qualifying product; (c) `review-images-modal.vue`, the other `VcNavButton` consumer, needs a review carrying **≥2 images** and none exists; (d) this store's **Datatrans is hosted-redirect, not secure-fields**, so that focus path has no surface here at all | Each is a live code path that cannot be observed, and (a) is the one that would fail a WCAG criterion outright rather than degrade. (b) is one catalog edit away from becoming untestable — delete or re-image that single product and a shared UI-kit primitive silently loses its only live coverage. All four currently resolve to "unverified", which is honest but indistinguishable from "fine" to anyone skimming | Seed durable a11y fixtures rather than relying on incidental catalog shape: one non-selectable BOPIS pick point, one product with ≥5 images, one review with ≥2 images. For Datatrans, either find a store configured for secure-fields mode or record it as a permanent coverage hole for this env so it stops being re-discovered each run |
| B-36 | **`prefers-reduced-motion` cannot be emulated on the visual lane, so any motion-gated assertion can only ever be derived, never observed.** Chrome DevTools MCP's `emulate` tool accepts `colorScheme` but has **no reduced-motion axis** — confirmed by direct attempt on 2026-09-10 (`reducedMotion:"reduce"` rejected as an unknown argument; the accepted set is `networkConditions`, `cpuThrottlingRate`, `geolocation`, `userAgent`, `colorScheme`, `viewport`, `extraHttpHeaders`). The VCST-5653 check was closed by reading the deployed CSS instead (the `.vc-button` transition and its `:active` `75ms` both sit inside `@media (prefers-reduced-motion: no-preference)`), which is sound but is a source read, not a behavioural one | Same shape as **B-26**: the visual lane is pinned to a tool that structurally cannot answer a whole class of question, and the resulting `PASS by derivation` / `BLOCKED` is easy to skim as observed coverage. Any accessibility work touching animation, transitions or autoplay inherits this | Allow the visual lane to run on a Playwright lane for motion checks — chrome and edge can both launch with `--reduced-motion=reduce`, and the visual axis' own guidance already lets its hard lane rule outrank the per-agent default table. Until then, mark motion-gated items explicitly as derivation-only rather than PASS |
| B-37 | **There is no domain map for the design-system / UI-kit surface**, so every run against it re-derives the same context by hand. `.claude/knowledge/domain/` carries maps for b2b-organizations, catalog, mobile-navigation, products, sales-rep, sitemap, store-settings and white-labeling — and nothing for the token layer, the UI-kit primitives or the preset pipeline. On VCST-5653 the surface inventory, the carrier taxonomy (five different code paths to one ring), the payment-iframe styling seam and — notably — **the fact that only Coffee and Red are WCAG-gated** all had to be established in-run. That last one lives in `vc-bug-catalog.md` as `VC-UI-001`, which is a bug catalog, not a map | `2g` recorded `domain_map: ABSENT`, which correctly blocks nothing, but the cost recurs per ticket and the most load-bearing fact (the gated-preset allowlist) is filed somewhere a reader would not think to look. A UI-kit map is also where the ring's own value chain and the 12 mixin call sites belong | Run `/qa-domain-map design-system`. The VCST-5653 test model (`reports/ba/test-models/VCST-5653-2026-09-10.md`) already contains most of Part 0 — value chain, variants, carriers, reverse edges, surface inventory — so the map is largely a consolidation rather than fresh derivation |
| B-30 | **A completed C1 regression run is not recorded into `history.json`, so `verify:gate --gate 5b` returns `CANNOT EVALUATE` (exit 2) for a run that demonstrably happened.** Observed 2026-09-10: `/qa-test VCST-5317` produced `RUN_ID: REG-2026-09-10-1453` with complete per-suite results, resolved CSVs, lanes, filters and a failure trace on disk under `reports/regression/REG-2026-09-10-1453/`, and `test-run-status.json` was closed out `status: completed` — yet the gate could find no history entry and could not compute a pass rate | The 5b verifier had to fall back to reading the raw results JSON by hand. It did so correctly and said so, but the gate that exists to make the pass rate machine-checkable contributed nothing, and its `CANNOT EVALUATE` is one careless reading away from being taken as a pass. A gate that cannot see a run that produced a full artifact set is worse than no gate, because it looks like coverage | Find where the C1 path diverges from a normal `/qa-regression` run in writing the `history.json` entry — most likely the `--ids` exact-set invocation skips the history append that a group-selection run performs. Either make the exact-set path append like any other run, or have `verify:gate` fall back to the run folder-s own `suite-*-results.json` when history has no entry, so the answer is a real pass rate rather than `CANNOT EVALUATE` |
| B-31 | **The pipeline forbids persisting the AC atomic-condition table, then asks a later gate to recompute percentages from it.** `/qa-test` §What persists marks `ac-analysis.md` **terminal-only, never written** (`.claude/rules/reports.md` §1), so `1d`-s per-condition table (C1–C20/C25) exists only in the orchestrator-s working context. Gate **5b** then requires *compute both percentages from the ACTUAL counts*. On 2026-09-10 the 5b verifier could not source the condition table anywhere under `reports/` or `.claude/`, reported that explicitly, and gave AC satisfaction at the coarse per-AC grain (2/6 strict, 4/6 counting substance) rather than inventing a per-condition breakdown | The two rules are in direct contradiction, and the resolution currently depends on the verifier being scrupulous. A less careful one closes the gap by accepting the orchestrator-s own numbers — which is precisely the ratification the verifier exists to prevent, since 5b-s independence rests on re-deriving from artifacts rather than from the doer. It also means the run-s headline AC figure is not reproducible after the session ends | Persist the condition table. The cheapest fix consistent with the reports policy is to make it a field of `summary.json` (which IS persisted and already carries `ac_analysis`) — an array of `{id, ac, assertion, impl_verdict, carrier}` — rather than a new report file. Then 5b has a citable source and the percentage is recomputable later. Whichever way it goes, one of the two rules has to give |
| B-29 | **The `MULTI_ORG_USER` fixture has drifted out of the shape a Critical case depends on.** Live-checked 2026-09-10 during `/qa-test VCST-5317`: the account now carries **one** `OrganizationMembership` row (org *Valvo branch in Vienna*, `f1d82c20-…`, unlocked, `Approved`, created 2026-08-26) and **12** `contact.organizations` entries — where the fixture is documented as **zero membership rows / 11 orgs**. Found by `test-data-engineer` while assessing whether the account could carry a locked membership (it cannot — see `DV-022`) | **`CUST-091` (Critical, suite `027-customer-orgs-invites.csv`) asserts the zero-membership shape as its load-bearing premise** for a global-`Contact.Status` block test, so the case is now testing something other than what it claims — and it fails in the direction that reads as a product defect rather than fixture drift. `DV-022` guards against *binding a membership mutation* to this fixture, but nothing guards the fixture-s own shape, so this drifted silently. Six suite files depend on this account (`027`, `008`, `006`, `011b`, `042`, `SMOKE-CHECKLIST.md`), none with lane isolation | Decide first whether the extra membership row was deliberate (someone provisioned it on 2026-08-26) or accidental. If accidental, remove it and re-run `CUST-091`. Either way add a shape assertion to `td:validate` — the fixture-s documented invariant (zero membership rows) is exactly the kind of derived constant the GOLDEN RULE says to gate rather than transcribe into a case-s Preconditions. Note this was found only because someone asked; no gate would have surfaced it |
| B-28 | **RESOLVED 2026-09-10.** ~~GOLDEN-RULE violation: test cases hardcoding evidence output paths.~~ Original count was wrong: a loose grep said *10 occurrences / 8 suites*; the precise scan found **28 occurrences across 10 suites** — 11 screenshot OUTPUT paths (`[ACT] capture … to reports/tickets/<Sprint>/<TICKET>/screenshots/<file>.png`) plus 17 provenance CITATIONS of ephemeral run artifacts (`summary.json`, `testing-checklist.md`, `findings-*.md`, `verification-report.md`) | A path in a case bakes the sprint and the ticket into a row that outlives both: re-run a sprint later it writes into a **closed** folder that `reports:prune` deletes, and **the case still PASSES**, so the evidence detaches from the run that produced it. It also spreads by copying — 3 of the 9 in suite `006` were authored an hour after the other 2, by an author correctly told to follow neighbouring house style | **DONE.** (1) All **28 → 0** removed across `006`, `007`, `011b`, `031`, `033`, `050j`, `057`, `083c`, `093`, `097`; the durable half of each citation (ticket key + field name) was kept and two spots where removal left broken prose were repaired by hand. (2) New **THIRD RULE** in `.claude/rules/test-data.md` — *evidence OUTPUT belongs to the evidence policy, never to a test case*; always-loaded set 55,325/80,000 chars, `context:check` green. (3) New **`DV-024`** guard in `scripts/test-data/validate-td-refs.ts` fails any suite row containing a literal `reports/tickets/…` path (escape hatch `DV-024-OK`); typecheck clean and **negative-tested** — a canary row produced `exit 1` naming the row, and `exit 0` once removed. (4) Separately, the meta-commentary bloat in the 3 rows this run authored was cut (11,034/7,544/6,777 → 8,630/4,484/5,815 chars; a normal row in the same suite is 845) — tooling essays about `enforce-real-user.mjs` and B-09 belong in the model, not in a case |
| B-27 | **`suites:review` passes a case whose Steps call a tool the harness hook-BLOCKS, so an un-runnable row lands in the corpus 0-Blocker/0-Critical.** Found on `/qa-test VCST-5317` Round 3 (2026-09-10): the newly authored `B2C-ORG-069` reproduces an in-flight refetch race via an in-page **fetch-interception shim driven by `browser_run_code_unsafe`**. `.claude/settings.json` matches that tool with `enforce-real-user.mjs`, which blocks it unless the JS matches a **read-only** allowlist (layout `getBoundingClientRect`/`getComputedStyle` audits, axe-core, `dataLayer`/`gtag`, cross-origin iframe inspection, the GraphiQL `insertText` paste). A shim that intercepts and delays responses **mutates page behaviour** and matches none of them. `suites:review` returned **0 Blocker, 0 Critical** on the appended rows; none of its 11 dimensions asks whether a Step's tooling is permitted | The row reads as coverage and is not. It would have been carried into a C1 `--ids` run and come back BLOCKED at execution, where a blocked-by-policy tool is easy to misread as a lane or environment problem rather than a case that was never runnable. Same **class** as B-21 (a gate green-lighting a case that cannot execute) but a different gate and a different axis — B-21 is about data resolvability at `tc:scaffold`, this is about tool permissibility at `suites:review`. Note the author chose the shim deliberately, to route around **B-09** (Chrome DevTools MCP can throttle but cannot authenticate; Playwright can authenticate but cannot throttle) — so the underlying condition is genuinely unreachable today and the case was a reasonable attempt at it | Add a `suites:review` dimension (or a `td:validate`-style scan) that greps authored Steps for MCP tools the active hook set blocks — `browser_evaluate`, `browser_run_code_unsafe`, `evaluate_script` — and fails unless the accompanying snippet matches `enforce-real-user.mjs`'s `ALLOWED_PATTERNS`. The hook already owns that regex list; the linter should import it rather than restate it, so the two cannot drift. Fixing **B-09** would separately make this particular case authorable for real |
| B-26 | **RECURRENCE 3, 2026-09-11 (`/qa-test VCST-5024`): all four axes `SKIPPED`, both profiles signed out — storefront AND admin.** A whole browser lane spent to learn it, again, on a `visual_surface: true` ticket whose single highest-value item was a `{DOC}` contradiction in storefront copy. Two refinements to the entry below. **(a) The viewport clamp did NOT reproduce** — a real 1280×900 desktop render was confirmed — so the `innerWidth: 500` limitation is intermittent or has been fixed; the auth half is the durable problem. **(b) The orchestrator's own brief named typing the admin password as the auth path, which contradicts the lane's no-`--secrets` rule** — the agent refused and reported the conflict rather than resolving it silently, which is the right behaviour but shows the trap is easy to walk into when writing the brief. The queued remedy this run: re-dispatch axes 1–2 to a **Playwright** lane with `--secrets`, which is exactly fix (2) below — note it cannot go to `playwright-firefox`, because that lane is barred from sign-in and Admin SPA work, so it must wait for chrome or edge to free. · **The `/qa-test` visual lane (`4v`) is structurally guaranteed to return `SKIPPED` for any authenticated or mobile surface, and has now done so two rounds running.** `.claude/skills/qa-test/visual-axis.md` pins the lane to **Chrome DevTools MCP**, which (a) has **no `--secrets`** — its documented remedy is a hand-signed persistent profile at `~/.chrome-devtools-mcp/vc-qa-profile`, and on VCST-5317 that profile was found **signed out on 2026-09-09 AND again on 2026-09-10**, so `/account` redirects to `/sign-in`; and (b) **clamps the viewport to `innerWidth: 500`**, confirmed on both rounds, so a `375×812` mobile axis cannot be measured on it at all. Net result across two rounds of a `visual_surface: true` ticket: **zero net-new visual signal**, twice, plus the one item unique to that lane (a throttled-network race, checklist A3.a) `NOT REACHED` both times | This is not a lane having a bad day — it is a routing rule that cannot succeed for a whole class of ticket. Any UI-visible ticket whose surface is behind a login (i.e. most of the B2B storefront) or below 500 px gets an agent dispatched, a browser lane consumed, and an all-`SKIPPED` report. The agent behaves correctly each time — refusing to type a credential and refusing to mint a throwaway account against a role-gated surface are both the right calls — so the cost is invisible in the verdict and shows up only as a report of blanks. It also silently degrades the ticket's evidence: a11y, design-system and `vs. DESIGN` all come back unassessed, and `SKIPPED` is easy to skim as clean. Related to and now partly superseding **B-09**, which logged the missing `--secrets`; the persistent-profile remedy landed but has **no owner and no step in any command**, so nothing keeps it signed in | Two parts. **(1) Make the profile's state a precondition, not a discovery:** have the lane probe auth **before** the agent is dispatched and, if signed out, either fail loudly with the one-line manual fix or skip the dispatch entirely rather than spending an agent to learn it. **(2) Allow the visual lane to run on a Playwright lane.** `.claude/rules/agents.md` forbids only `playwright-firefox` for click-driven work; chrome and edge both carry `--secrets` and neither clamps the viewport, and the visual axis' own guidance already lets its hard lane rule outrank the per-agent default table. Until one of those lands, `visual_surface: true` should be treated as advisory for authenticated surfaces — it currently promises coverage it cannot deliver |
| B-21 | **`tc:scaffold --check` green-lights a case bound to a `@td()` alias that does not exist.** Found during `/qa-test VCST-5317` Round 3 (2026-09-10). The `1e-plan` authoring plan carried `@td(MULTI_ORG_USER.email)`; the gate returned `OK — 4 row(s) would be scaffolded`. There is no `MULTI_ORG_USER` alias in `test-data/aliases.json` or `aliases.vcst.json` — the real fixture is env-var-based (`{{MULTI_ORG_USER_EMAIL}}`), which is precisely the confusable pair. The KEEP gate checks `observable` / `defect` / `plausible` and never that the `data[]` tokens **resolve** | `1e-plan` exists to cull an undecidable case **before** it is written, and this is the one class it waves through — a case that cannot execute at all. It was caught only because the `3a` brief happened to ask `test-data-engineer` to resolve every token explicitly; with `data_surface: false`, or a 3a dispatch that did not ask, the row would have been authored, appended to a suite CSV, and only failed at `4c` looking like a product or fixture defect | Have `tc:scaffold --check` resolve every `data[]` token through the same resolver `td:validate` uses and fail on an unresolvable `@td()`. A bare `@td(NAME.field)` whose `NAME` matches no alias but whose `{{NAME_EMAIL}}` form does exist should produce a *named* error suggesting the env-var form, since that is the actual mistake being made |
| B-22 | **The Test Model is designed to be amended in place across rounds, but is capped at 260 lines, so each round must delete history to add findings.** `reports/ba/test-models/VCST-5317-2026-09-09.md` stood at **258/260** when Round 3 opened. Fitting the round in required hand-compressing superseded Round-2 prose (an evidence table → one line, two `R2.x` bullet blocks → one line each); that compression left an **orphaned duplicate line** which needed a second pass to catch. `/qa-test` §1e mandates amend-never-fork, and `.claude/rules/reports.md` §2 sets the cap — both correct in isolation | The two rules pull against each other and the tension grows monotonically: every round adds findings and the only way to fit them is a manual deletion of prior evidence, done by whoever is mid-run and least placed to judge what is still load-bearing. The failure mode is silent (a compressed line reads fine) and lossy (Round 1's per-condition results are already unrecoverable). A third round on any long-lived ticket hits this again, harder | Pick one: raise the cap for `reports/ba/test-models/` specifically (it is a durable fault model, not a report anyone skims on a deadline — the caps exist for the latter), or define what a round MAY compress (e.g. rounds older than N collapse to a fixed-size summary block, mechanically), or move superseded rounds to a `-rounds.md` appendix the model cites. Whichever is chosen, it should be a rule the amender follows, not a judgment call made under a line budget |
| B-23 | **Two writers are authorized on one Test Model file, which the repo forbids for suite CSVs and is silent about here.** `/qa-test` Step 3x's contract routes *model amendments* to the discovery lane ("amend, never fork"), while the orchestrator is simultaneously applying its own `1e` amendments to the same file. Observed on VCST-5317 Round 3: both the orchestrator and the `3x` agent held a write mandate on `VCST-5317-2026-09-09.md` at the same time. No loss occurred **only because** `3x` declined to write (it hit the 260-line cap and returned proposals instead) | `.claude/rules/regression.md` §Suite inventory states the single-writer rule for suite CSVs and gives the measured reason; a Test Model is the same shape of artifact — one file, long lines, no meaningful merge — and carries no such rule. A read-modify-write from each side silently drops one set of amendments, and a model is exactly where that is hardest to notice, because nothing lints it | State the single-writer rule for `reports/ba/test-models/*.md` in `/qa-test` §3x and in the `qa-test` skill: **the discovery lane RETURNS proposed amendments; the orchestrator APPLIES them at the join.** That is what happened here by accident and it is the correct design — 3x's own report already emits them in an applyable form |
| B-24 | **`seed:membership-lock:verify` exits 1 for a state the run deliberately seeded, so it is unusable as a mid-run health signal.** It treats **V1 (everything unlocked) as the only resting state**, so once a run has legitimately applied V2/V7 the verify reports `VERIFY FAILED: N locked leg(s)`. Observed twice in one run (2026-09-10): the `3a` agent explained it in its report, and the orchestrator explained it again an hour later for the frontend lane. The read-back detail it prints IS correct and IS what you want — only the exit code and the `FAILED` wording are wrong for the situation | The one command that confirms a lock fixture landed cannot distinguish *"residue from a crashed run"* from *"the state this run just asked for"*. Every consumer therefore learns to read past a red `VERIFY FAILED`, which is exactly how a real residue leak gets waved through later — and residue on this fixture is not cheap, since `IMPERSONATE_TARGET_BLOCKED` sits in the same table and suite 082 depends on it | Add `--expect <V1|V2|V4|V7>` (and/or `--mid-run`) so the verify asserts the **intended** state and exits 0 when the read-back matches it, reserving a non-zero exit for a genuine mismatch. Keep bare `--verify` meaning "assert the resting state" for teardown. Cheap, and it makes the teardown gate trustworthy again |
| B-25 | **This backlog file has duplicate ids: `B-13` and `B-14` each appear twice** (`td:reconcile` BIKE-* products vs. the chrome/edge HAR sweep; the stale `LOYALTY_*` GUIDs vs. the RESOLVED `SR-FE-013a` entry). Two of the four were added in a later batch that did not check the existing maximum | The file's own convention is one-liners referenced by id ("Move a row to Resolved with the date"), and B-14 already demonstrates the problem — one B-14 is marked RESOLVED while the other is open, so "B-14 is done" is now ambiguous in exactly the file meant to be the shared worklist. Same class as the `CATA-`/`ORDA-` case-id collision the suites gate now enforces against | Renumber the two later duplicates to the next free ids (B-26, B-27) and note the remap inline so existing references stay followable. Then add the file to whatever `context:check` already walks — a one-line uniqueness assert over `^| B-d+ |` would have caught both at the commit that introduced them |
| B-13 | **`td:reconcile` [7]+[8] fail on 4 configurable BIKE-* products that carry no `AGENT-TEST` prefix and no SEO title/store** — `BIKE-OPT-SPORT-20260725` ("Sport Wheels"), `BIKE-OPT-STD-20260725` ("Standard Wheels"), `BIKE-CFG-20260724` ("Test Config Bike 20260724"), `BIKE-CHK-CFG-20260725` ("Test Config Checkout Bike 20260725"), all in catalog `AGENT-TEST-SEED-Configurables`. Observed on vcst-qa 2026-08-05; **8 of the 10 hard `td:reconcile` failures on that env are these 4 products counted twice** (once per check) | `td:reconcile` is red for everyone on vcst, so the live gate cannot be used as a pass/fail signal — a *new* seeding regression lands in a list that is already failing and gets skimmed past. Per check [7]'s own wording, `seed:configurable:teardown` would also **orphan** all 4 (the sweep is prefix-driven), so they accumulate across reseeds | Rename the 4 products to the `AGENT-TEST-` prefix in whichever spec creates them (`products/configurable-specs.mjs`) and give them the same `buildStoreSeo()` treatment the other seeded products get, then reseed. Until then the env cannot reach a green `td:reconcile`, which is the gate `.claude/rules/test-data.md` names as the post-seed check |
| B-14 | **Two stale `LOYALTY_*` GUIDs in the committed `test-data/aliases.vcst.json` overlay** — `LOYALTY_NOBAL_USER.memberId=31399071-…` and `LOYALTY_WHOLESALE_USER.memberId=47db2112-…` are not live members (`td:reconcile` [11]). The fixtures were torn down and re-created with new GUIDs and the overlay was never refreshed | Exactly the silent failure check [11] was built to catch: `@td(LOYALTY_NOBAL_USER.memberId)` resolves to a **deleted** entity, so the assertion never matches and the case reads as a *product* bug rather than stale data. The overlay is committed, so every teammate and CI run inherits it | Re-run the owning seeder on vcst (`TEST_ENV=vcst npm run seed:loyalty-fixtures` / `seed:loyalty:users`, whichever owns these two aliases) and commit the refreshed overlay. Worth checking why the teardown/reseed path did not write back — if the seeder does not call `writeEnvAliasOverride` for these two, the drift will simply recur |
| B-15 | **`salesRepTopSellers` hard-errors unless `storeId` is passed, and `093 SR-HD-010` tells the tester to omit it.** Verified live on vcst-qa 2026-08-05 as `SR_REP_PRIMARY`: `salesRepTopSellers(period, take)` → `INVALID_OPERATION` "Error trying to resolve field"; adding `storeId` → 6 rows. `organizationId` alone does **not** help (still `INVALID_OPERATION`); `organizationId + storeId` works. Reps serving exactly one org (`SR_REP_ACME2`) return `0 rows` for every shape rather than erroring, so the error is specific to a multi-org rep with no store context | `SR-HD-010`'s step says *"issue `salesRepTopSellers(organizationId omitted, take: 25)` directly as SR_REP_PRIMARY"* — followed literally that call errors, and the case's `take`-cap assertion reads as a backend defect when it is a missing required argument. The storefront widget evidently always passes `storeId`, so the UI half of the case passes while the API half fails | Decide which side is wrong: either `storeId` should default from the caller's token (product fix — an `INVALID_OPERATION` with no message naming the missing arg is poor DX regardless), or the case should say "organizationId omitted, **storeId supplied**". At minimum amend `SR-HD-010`'s `[GQL]` step so the API assertion is runnable, and note the arg requirement in `knowledge/api/graphql-schema.md` |
| B-01 | `036-bopis-store-selector.csv` references 5 undefined aliases — `BOPIS_BVA_50/51/100/101/151` | `npm run td:validate` fails **repo-wide**, so the gate is red for everyone and those 5 cases cannot resolve their data | Author the aliases in `test-data/` (BVA quantities for the store selector) or rewrite the cases to use `@td()` refs that exist |
| B-03 | `bl:lint` reports ~99 Medium + 8 Informational — most of them false-traceability findings, plus 2 non-contiguous id sequences | Not blocking (0 Blocker), but the noise floor makes a real regression hard to spot | Triage the false-traceability findings — likely a linter matching rule, not that many real gaps |
| B-04 | `bl:lint` is **not wired as a gate** anywhere | This is why the oracle could sit visibly broken (two concatenated copies, 150 Blocker findings) without anyone noticing | Add `bl:lint` to the CI gate set, at minimum failing on Blocker |
| B-08 | The Invariant Coverage Summary in `business-logic.md` carried **two contradictory `**Total**` rows** (`169/50/94/25` and `159/50/86/23`), neither matching the file's real content | The oracle's own headline numbers were wrong in two different ways at once | **Fixed on the VCST-5367 branch** — collapsed to one row computed from the domain rows: `188 / P0 51 / P1 103 / P2 34`, which now equals the actual heading count. Worth a lint rule so the table can't drift again |
| B-05 | Mid-drag visual states and manual mouse choreography have **no permitted tooling path** — `browser_evaluate` / `run_code_unsafe` are hook-blocked by design, and the tool-level drag helper cannot hold a drag open | A small set of visual assertions (`.sortable-ghost`, dark-mode `shadow-xl`, drop-zone treatment) is permanently unverifiable | Either a narrow, reviewed exemption in `enforce-real-user.mjs` for mouse-down/move/up choreography, or a tool-level primitive. Owner decision, not a unilateral fix |
| B-09 | **Chrome DevTools MCP has no `--secrets`, so throttling/emulation is unusable on any authenticated page.** R-10 bound the server, but the four Playwright servers carry `--secrets .env.playwright.local` and Chrome DevTools does not — and it launches its own Chrome with no session (verified: `/company/dashboard` → redirect to `/sign-in`). Throttling exists only in the browser that cannot log in; authentication exists only in the browsers that cannot throttle | This is the *sole* remaining blocker for 5 cases whose Preconditions require it: `SR-HD-034` (skeleton under slow 3G), `SR-HD-035`/`SR-CP-047` (failed layout read), `SR-HD-036`/`SR-CP-048` (failed layout save). They read as product uncertainty but are a tooling gap. It also blocks `lighthouse_audit` on any signed-in page | Either give the Playwright servers a network-throttling / request-interception primitive (the cleanest fix — auth is already solved there), or expose a shared CDP endpoint: add `--remote-debugging-port` to the Playwright chrome launch and point `chrome-devtools-mcp` at it with `--browser-url`, so one authenticated browser is drivable by both. Do **not** solve it by typing password literals into the DevTools browser |
| B-10 | **RESOLVED 2026-09-14.** ~~**`config/mcp-playwright-mobile.config.json` reintroduces the HAR-path bug**~~ — `recordHar.path` is `./test-results/mobile/har` with **no `.har` extension**. This is the exact defect `.claude/rules/reports.md` §6 documents as fixed for chrome/edge/firefox on 2026-07-30 (`…/har/session.har`); the new mobile config created under R-10 was written in the old broken shape | Playwright treats `recordHar.path` as a *file*, so the mobile lane writes a real 1 MB+ archive to a file literally named `har`, invisible to every `*.har` glob — tooling and reviewers conclude HAR capture is disabled for the mobile lane | One-line fix to `./test-results/mobile/har/session.har`, matching the other three lanes. Needs an MCP restart to take effect. Worth a lint/unit check over `config/mcp-playwright-*.config.json` asserting `recordHar.path` ends in `.har`, since this has now regressed twice **DONE 2026-09-14 — both halves.** (1) The path now reads `./test-results/mobile/har/session.har`, matching the other three; verified across all four configs. (2) The lint/unit check this row asked for exists: **`scripts/unit/playwright-lane-configs.test.mjs`** asserts `recordHar.path` ends in `.har` and is lane-scoped — plus the sibling invariants that fail just as silently: every lane records video into its own directory (`reports-policy.md` §5.2), no two lanes share an `outputDir`/HAR/video path (one lane would overwrite the other's evidence), each lane is `isolated` and pinned to `en-US`, the Firefox `window_occlusion_tracking` pref survives (without it every click on that lane times out), and no lane is WebKit. 7 tests, **negative-tested**: reverting the mobile path to `…/har` fails with the explanatory message and passes again once restored. Separately on the same day the mobile config gained `recordVideo` (390×844) and a `playwright-mobile` entry was added to the gitignored `.mcp.json` — that lane had never been bound, so it had never recorded anything; unverified until the next MCP restart |
| B-11 | **`seed-sales-rep.mjs` cannot self-heal a TEMPORARY account lockout, so a few bad logins block provisioning until the lockout expires.** `user-provision.mjs` has exactly this self-heal (`hasStaleLockout` → clear `lockoutEnd`/`lockoutEnabled`/`accessFailedCount`, `ensureSecurityAccount`), but the sales-rep seeder provisions through the module's own `POST /api/sales-rep` + `resetSecurityPassword`, which never touches lockout state. Observed 2026-08-04 on vcst-qa: `SR_REP_PRIMARY` sat at `lockoutEnd=2026-08-04T21:22:15Z` after a handful of failed logins; a full `seed:sales-rep` ran green and reset the password, yet `/connect/token` still returned `user_is_temporary_locked_out`. **Also note the working unlock shape:** `POST /api/platform/security/users/{GUID}/unlock` succeeds, while `PUT /api/platform/security/users` with a cleared `lockoutEnd` returns `{"succeeded":true}` and **silently no-ops** — a 200 that changes nothing (consistent with the `reference_token_endpoint_org_auth_testing` GUID-not-name note) | A green seed is not proof the fixture can authenticate, which is the one thing the seed exists to guarantee. The failure surfaces later as an `invalid_grant`-looking auth error in a browser/suite run and reads as a credential or product problem rather than a leftover lockout. Self-inflicted lockouts are routine on shared QA envs (abuse/lockout suites, a mistyped password) | In `ensureRep`, after `resetSecurityPassword`, clear a NON-permanent lockout via `POST /api/platform/security/users/{userId}/unlock` — guarded by the same `hasStaleLockout` rule so `SR_REP_BLOCKED`'s deliberate `9999-12-31` lockout is never cleared. Better: lift the clear-lockout step out of `user-provision.mjs` into a shared helper both paths call, so the two provisioning routes cannot drift again |
| B-12 | **No seeded sales rep has a phone, but suite `090` asserts the Phone cell is non-empty.** `090-sales-rep-my-sales-reps-storefront.csv` asserts *"the row's Phone cell shows a non-empty phone value {DOC}"* and cross-checks `customerSalesReps items[].phones[0]`, yet `test-data/sales-rep/sales-reps.csv` has no phone column and `seed-sales-rep.mjs` never sets one. Verified live on vcst-qa: `GET /api/members/796b58cc-…` (Priya Rao / `SR_REP_PRIMARY`) returns `phones: []`, and `customerSalesReps` returns an empty `phones` | That assertion can only ever FAIL, and it fails in the way most likely to be misread — a blank Phone column is exactly the rendering bug the case was written to catch, so a data gap masquerades as the product defect. The case's own "unexpected" column even names *"Phone column always blank even when phones[] is populated"*, a state the fixtures cannot currently reach | Add a `phone` column to `sales-reps.csv` (a fixed non-PII fixture value, e.g. the `+1-206-555-01xx` range already used for `OWNER_PHONE`), set it in `ensureRep`'s create/update body, and extend `validate-sales-rep-data.mjs` to require it non-empty for every `seeded=true` rep. Cheap, and it unblocks the assertion rather than weakening it |
| B-13 | **HAR capture produced nothing on the chrome and edge lanes during the VCST-5586 run** — the same class B-10 flags for mobile, but on the two lanes `.claude/rules/reports.md` §6 records as *fixed* on 2026-07-30. Edge is blocked by a leftover legacy **file** named `test-results/edge/har` (448 KB, from before the fix), so the configured `har/session.har` **directory cannot be created**; on chrome, `test-results/chrome/har/` simply never appeared and the archive never flushed | Two full browser sessions across three storefront pages produced zero HAR, while the evidence policy says "HAR always". Worse, it fails silently — the lane looks configured, the config is correct, and only a reviewer looking for the file finds out. The 2026-07-30 fix is therefore only *half* landed: the config was corrected but the stale artefacts were never swept | Sweep the legacy path artefacts (`test-results/{chrome,edge,firefox}/har` as files → rename to `session-legacy.har` inside the directory, as was done for the others), and add a preflight assert that `recordHar.path`'s parent is creatable — a lane that cannot write its HAR should fail loudly at startup, not silently at teardown. Pairs with B-10's proposed `*.har` extension lint |
| B-14 | **RESOLVED 2026-09-04.** ~~**`089` `SR-FE-013a` documents a redirect that no longer happens.**~~ The case states a zero-scope sales rep (no served customers) is redirected to `/account/dashboard`; observed live on vcptcore-qa 2026-08-05 that `agent-test-sr-nocustomers@example.com` **lands on `/company/my-customers` and renders the `"No customers found"` empty state** — no redirect | A stale precondition in one case silently propagates: the new `SR-FE-054` was authored against it and had to be reconciled at execution time. Either the product changed and the case is stale, or the redirect regressed — and the case as written cannot tell those apart | Triangulate `SR-FE-013a` (dimension 11 / `--triangulate`): confirm against the sales-rep routing source whether the redirect was intentionally dropped. If dropped, rewrite the assertion to the empty-state behaviour; if it regressed, that is a product bug and needs its own report. Do not simply delete the case. **DONE 2026-09-04 (TCA-2026-09-04):** triangulated three axes — the redirect was **intentionally dropped by VCST-5494** (`myCustomersRoute` now sets `meta requiresOrganization:false`, so a globally-permissioned rep with zero served orgs clears the generic `/company` gate), live-confirmed no redirect with the hub rail visible. **Not a regression, so no product bug.** `SR-FE-013` was re-based (title, preconditions, both assertions, cross-layer check, failure signals) to assert ADMISSION rather than redirect; the case was not deleted. The phantom id **`SR-FE-013a` never existed** — its citations in `SR-FE-054`, `SR-FE-021`, `scripts/lib/user-roles.mjs` and `test-data/sales-rep/sales-reps.csv` were all corrected to `SR-FE-013`. Note this finding was logged on 2026-08-05 and sat a month unactioned — see the open `project_no_change_triggered_staleness_loop` workstream |
| B-07 | **Suite `080` was retired upstream — and 18 of its cases have no home.** Commit `cf460246` (2026-07-31) retired it on the grounds that "`full`/`sprint` cover the release scope". An independent case-by-case comparison of all 100 `FRR-*` cases against the other suites (3,861 cases) says that is **82% true**: **82 DUPLICATE / 15 PARTIAL / 3 UNIQUE** | Retiring 080 was right — but the 18 non-duplicate cases were lost with it, and several are P0-adjacent | **Port the 18 into the domain suites.** The 3 with no equivalent anywhere: **`FRR-090`** full cross-layer purchase flow as one continuous chain *including inbox email verification* (closest analogs are BOPIS-only or B2B-only), **`FRR-087`** feature-flag toggle takes effect without a restart (`BL-CROSS-006` is cited widely but only on generic admin-edit smoke cases), **`FRR-059`** an Admin-hidden product must stop being searchable while its category stays browsable. Notable PARTIALs: `FRR-031` free-shipping threshold recalculating **on the storefront** (the only other `BL-SHIP-003` case is Admin-config-only), `FRR-040`/`055` actual **inbox** email delivery + content match, `FRR-094` a **customer-initiated** return (suite 073 is 100% Admin-side), `FRR-078`/`079` a full-journey a11y walk (suite 045 is component-scoped). Recover the content from `git show 744b374d:regression/suites/_release/080-full-regression-release.csv` |

| B-16 | **`playwright-firefox` MCP lane is broken — `requestAnimationFrame` never fires**, so every click/hover/type actionability check times out. Hit during `REG-2026-08-06-0234` (suite 088): 15 cases stalled on firefox; 9 were re-run cleanly on `playwright-edge`, proving it is the lane and not the cases | A firefox-lane run does not fail loudly — cases come back BLOCKED/timed-out and read as product or data problems. Firefox is 1 of the 3 pool slots, so roughly a third of any parallel regression is affected, and the browser-fallback chain masks it just enough to keep the run "green-ish" | Reproduce outside the harness first (plain `npx playwright open --browser=firefox` against the storefront) to separate an MCP-config problem from a Playwright/Firefox version problem. Until it is fixed, either drop firefox from the pool in `config/test-suites.json` or make the runner treat an all-cases-timeout on one lane as a RUN-level failure instead of per-case BLOCKED — a silent lane is worse than a missing one |
| B-17 | **Suite 088 rows `REV-SF-001..011` carry 6 Critical + 34 High linter findings** — invalid `Automation_Status: "manual"` (lowercase, a value used nowhere else in the repo), state-changing `[ACT]` steps with no `[WAIT]`, generic element references, fewer than 2 `Failure_Signals`, and missing requirement links. Surfaced by `suites:review` while promoting the 17 new VCST-5487 cases; the new rows introduced none of them | The deterministic gate on this suite is effectively unusable — a real regression in a *new* row is buried under 40 pre-existing findings, and `--fail-on=High` can never be turned on for the file. Same shape as R-06 (`091` had `"verified"`), so this is a recurring pattern rather than a one-off | Mechanical for the most part: normalize `manual` → `Manual`/`Reviewed` per R-06's precedent, add the missing `[WAIT]`s and second `Failure_Signals`, and link each High case to its originating story. Worth one pass so `--fail-on=High` can be enforced on 088 going forward |
| B-18 | **Two suite-088 cases navigated to `{{FRONT_URL}}/product/{{TEST_SKU}}`** (`REV-SF-014`, `REV-SF-018`) — a path the storefront answers with HTTP 200 and a client-side 404, so the assertion "no rating row is shown" passed **vacuously** on a page that has no product at all. Fixed inline during the VCST-5487 run by pointing both at a new `@td(REV_PROD_NO_REVIEWS.slug)` fixture | This is exactly the SPA soft-404 trap `.claude/rules/test-data.md` already warns about, and it defeats the kind of negative assertion that is hardest to notice failing. Fixed here; logged because the *pattern* is likely to exist elsewhere | Sweep the corpus: `rg "FRONT_URL}}/product/" regression/suites/`. Any hit is either vacuous or relying on a redirect that is not guaranteed. Consider a `td:validate` scan (a new DV-0xx rule) so a future case cannot reintroduce it |

| B-19 | **Seeded sales-rep orders carry a null `OrderLineItem.CategoryId`, so the Top Sellers category filter has almost no standing coverage.** Found during the VCST-5590 run on vcptcore-qa: 4 of 5 top-seller products returned `categoryId: null`, and three of the rep's four customers offered **zero** category chips. An order placed through the real storefront checkout (`addItem` → `addOrUpdateCartShipment` → `addOrUpdateCartPayment` → `createOrderFromCart`) carries the category correctly — so this is the seeder shortcutting order creation, not a product defect | The whole `salesRepTopSellerFilterRules` vocabulary is derived from `DISTINCT OrderLineItem.CategoryId`. With the seeded data it can only ever produce one chip, so the feature's main axis is untestable from standing fixtures — the VCST-5590 verification had to seed a live order mid-run to prove it at all. A future regression that drops categories entirely would look identical to the current data state | Set `CategoryId` on the line items the order seeders create (it is available on the product being seeded), or have the sales-rep order fixtures go through the storefront checkout path the way `seed-sales-rep-stats.mjs` already does for carts. Then add a `td:validate:sales-rep-stats` assertion that at least two DISTINCT top-level categories are reachable across the rep's book, so the filter keeps real coverage |
| B-20 | **Top Sellers "Revenue" is pre-discount list price × units, not money received.** Observed on order `CO260806-00001` (vcptcore-qa): the skis line was sold at $549.00 each (list $799.00) yet Top Sellers reports revenue $2,397.00 = 3 × $799.00. Confirmed **pre-existing** by diff — `SalesRepTopSellerService` already grouped on `x.Price` before vc-module-sales-rep PR #9, which only narrowed the projection | Not broken math (the order's own subtotal/discount/total reconcile), but a rep reading a column labelled *Revenue* will read it as earnings; on this order it overstates by $1,953.47 (~194%). It is also a silent divergence — nothing in the UI signals the figure is pre-discount | Product decision first, then either rename the column / add a qualifier, or switch the aggregate to the discounted basis (`PlacedPrice`, or `ExtendedPrice` which already folds quantity). Not a VCST-5590 defect — raise separately against the statistics module so the choice is recorded rather than inherited |

## Resolved — 2026-08-04 (found during the VCST-5367 lifecycle run)

These were fixed inline, which is what prompted this file to exist. Kept for the record.

| # | Finding | Why it mattered | Fix |
|---|---|---|---|
| R-01 | **Duplicate suite id `092`** — two different suites shared it, and the resolver is `suites.find(s => s.id === id)`, first match wins | `092-sales-rep-admin.csv` (18 cases: permissions, roles, rep lifecycle) was **unreachable — it had never run** | Embedded-app suite renamed to `092b` (`git mv`); both registered in the `sales-rep` selection |
| R-02 | **Suite `093` was never registered** in `config/test-suites.json` | 18 existing hub-dashboard cases never ran and were in no selection | Registered; added to `sales-rep`, excluded from `full`/`sprint` consistently with its siblings |
| R-03 | **Corrupt row in `050m`** — an incomplete duplicate of `SR-GQL-034` with an unterminated quote | Broke CSV parsing for the **whole file**: `csv-parse`, `graphql:lint-labels` and `suites:review` all failed, so 050m had **no working deterministic gate**. This one genuinely blocked the task | Orphan row removed (content was fully redundant) |
| R-04 | **Duplicate case id `SR-GQL-033`** — two fully-formed cases shared it | Ambiguous cross-references | Cross-references all pointed at the "fails CLOSED" case, which keeps `033`; the other became `SR-GQL-118` |
| R-05 | `089` `testCount` drift — manifest 42 vs 48 real | Under-reported scope | Corrected; `totalSuites` reconciled to 121 |
| R-06 | Non-standard `Automation_Status: "verified"` on 11 rows of `091` — a value used nowhere else in the repo | 11 linter findings | Normalized to `Reviewed` |
| R-07 | **Release suite `080` deleted from disk** by `9dd9f3e3` (a commit about test-data) while its manifest entry stayed — the **second** deletion (`b4b03bf8` deleted it, `744b374d` restored it) | Worse than a dangling selection: `/qa-regression release` resolved a **missing file and reported a green run over ZERO cases** | **Resolved upstream, better than my first attempt.** I restored the file; upstream's `cf460246` had already **retired** it deliberately *and* hardened the tooling — a declared-but-absent CSV is now an unconditional hard failure, never baselineable, and `CSV_LINT_BASELINE` was emptied. I dropped my restore in favour of upstream's decision. The residual gap is B-07 |
| R-08 | **`business-logic.md` was two concatenated copies of itself** since merge `02ca3da2` (2026-07-28) — 334 invariant headings for 184 ids, 150 Blocker findings, and two *different* "Domain 20"s | Every agent reading the primary QA oracle saw each invariant twice and could hit either of two conflicting Sales Rep domains | **Fixed upstream independently, and differently.** Both sides reconciled it; upstream renumbered `BL-SREP-001..003` into `BL-SR-012/013/014`, where my run had preserved them as `Domain 20a`. Upstream's shipped decision wins, so my reconciliation was dropped and my 18 layout invariants were renumbered `BL-SR-015..032` to clear the collision on `BL-SR-014`. Kept for the record: `reports/knowledge/BL-RECONCILE-2026-08-04.md` |
| R-12 | `suites:lint` burn-down baseline was stale — `015`/`026`/`044`/`045`/`052` were listed as malformed but passing, and `080` was being used to suppress a *deleted* file | `CSV_LINT_BASELINE` conflated "unparseable" with "absent", permanently exempting a suite that no longer existed | **Resolved upstream** by `cf460246`: `findMissingFiles()` split out as an unconditional hard failure, and the baseline emptied |
| R-09 | `playwright-firefox` and `playwright-edge` were missing `--secrets .env.playwright.local` while `playwright-chrome` had it — CLAUDE.md requires it on **each** server | A secret NAME typed in firefox/edge went into the form **as a literal**; this is what blocked the cross-device test case | Both aligned. `--secrets` is read at MCP startup, so it takes effect next session |
| R-10 | No Chrome DevTools MCP and no touch-enabled Playwright profile were bound, although several test cases' own Preconditions require them ("devtools network interception", "throttle network to slow 3G", mobile/touch viewport) | Five test cases were unverifiable and looked like product uncertainty when they were a tooling gap | `Chrome DevTools` added to `.mcp.json` verbatim from `templates/.mcp.json.example`; new tracked `config/mcp-playwright-mobile.config.json` (390×844, `hasTouch`, `isMobile`) + a `playwright-mobile` server. Effective next MCP restart |
| R-11 | Coverage Summary table in `business-logic.md` was stale independently of R-08 — the Cart row omitted `BL-CART-015`, and the Total row did not match the sum of its own columns | The oracle's own headline numbers were wrong | Recomputed from the deduped set: 184 / P0 51 / P1 100 / P2 33 |

## Open — 2026-09-08 (found during the VCST-5652 `/qa-test` run on vcptcore-qa)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-43 | **`sizedControlAuditSnippet` + `classifySizedControl` exist in `scripts/lib/measure-layout.ts` but are wired to NO runner probe tag** — `scripts/layout/layout-runner.ts` dispatches only `PROBE:{CLS,SPACING,ALIGN,OVERFLOW,TOUCH,IMAGES,IMGDIMS}` | A whole class of assertion — *"this control renders at the size its own design token says"* — is **not expressible** in a runner-native suite. Concretely it blocked VCST-5652 from landing durable coverage in `048c`: the ticket's entire subject is a per-size icon token, and there is no way to assert it. The library function is written and even documents the Storybook-vs-storefront diff workflow, so this is pure wiring | Add `case "PROBE:SIZED"` → `st.sized = await page.evaluate(sizedControlAuditSnippet(sel, {tokenVar}))` plus a `SIZED` assertion arm calling `classifySizedControl`, an arg grammar for `tokenVar=` (e.g. `[PROBE:SIZED] .vc-button .vc-icon \| tokenVar=--vc-icon-size`), the header grammar doc, and the `probes:` output at ~line 762. Then author the VCST-5652 icon-size cases into `048c` |
| B-44 | **`[PROBE:ALIGN]` compares ALL matches document-wide, which makes it unusable for a per-component row without a unique anchor** — `alignmentAuditSnippet` takes max−min centre across every match of the selector | Authoring a natural-looking case like `[PROBE:ALIGN] .vc-button--size--sm .vc-button__content > *` yields a **phantom 1557px drift** (measured live on `/search`): it paired an icon from one button with the label slot of a different button elsewhere on the page. This is a false-FAIL generator aimed straight at anyone writing a component-alignment case. Compounding it, storefront VcButtons carry almost no `data-test-id` (1 of 5 on a PDP), so there is often no unique anchor to scope to | Either document loudly in the `layout-runner.ts` grammar header that ALIGN selectors MUST resolve to exactly one row, or make the snippet group matches by their common parent and report the worst per-group drift. Also worth adding `data-test-id` to the ui-kit's labelled icon buttons |
| B-45 | **A rotating element's `getBoundingClientRect` is not a size measurement** — the loading-spinner check in ad-hoc layout snippets reads one instantaneous AABB of a `animate-spin` square | Produces a **false failure of up to 41%** (√2) whenever it samples off-axis. Observed live: a 16px spinner measured 16.04→22.60px across 23 samples. Any suite case that asserts spinner size this way will flake | Take the **minimum across samples**, or read `offsetWidth` / the untransformed box, before treating a loader size delta as a defect. Worth encoding in `measure-layout.ts` as a helper so it is not re-derived per case |

## Open — 2026-08-08 (found while unblocking the `REG-2026-08-07-1446` resume)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-25 | **`npm run seed:users` aborts the whole 54-account run on one account's password policy.** `TEST_ENV=vcst npm run seed:users` dies at the first *create* — `create kutasina.elena+user22@gmail.com: ["Passwords must have at least one non alphanumeric character."]` (`env-role:USER2`, var `USER2_PASSWORD`) — and the remaining 53 accounts are never touched. `USER2_PASSWORD` is also one of the three vars `td:validate:credentials` [5] already warns is undocumented in `templates/.env.local.template` / `.env.defaults` | The personal-account seeder is the sanctioned repair path for credential drift (`ensurePersonalAccount` force-reconciles a *declared* password onto an existing account). One env var that violates the platform's password policy makes that entire repair unreachable, and the failure names a single account so it reads as a one-account problem rather than a total abort. It is why B-26 below had to be repaired with a hand-written targeted script instead | Two independent fixes: (a) set a policy-valid `USER2_PASSWORD` and document it in `templates/.env.local.template` alongside `USER_PASSWORD`/`MULTI_ORG_USER_PASSWORD` (the other two [5] warnings); (b) make the seeder **non-fatal per account** — collect failures and report them at the end, the way `reconcilePasswordSafe` already does for password reconciliation, so one bad row cannot cost the other 53 |
| B-26 | **Declared credentials have drifted from the live vcst-qa accounts at scale: 59 of 69 declared storefront logins fail `/connect/token` with `login_failed`.** Probed 2026-08-08 against `B2B-store`: **every** `test-data/b2b/users.csv` account (john.mitchell, emily.johnson, carlos.rodriguez, hans.mueller, …) and **every** `test-data/users/test-users.csv` account (`qa-user-01…10`, `qa-vip`, `qa-intl-*`, …) rejects the password the repo declares for it. The accounts exist, are `Customer` / `Approved` / `storeId=B2B-store` / not locked — only the password disagrees. The 2 non-`login_failed` failures are intentional lockout fixtures (`qa-locked`, `AGENT-TEST-imp-target-blocked`) and are correct | This is the root cause *behind* B-21's symptom, and it is much larger than the three lane accounts: any suite that signs in as a B2B member or a generic QA user cannot authenticate, which is what produces BLOCKED-at-login cases that read as environment or product problems. `td:validate:credentials` passes (0 hard problems) because it only checks that each account has **one** declared password — nothing verifies that declaration against the live env | Add a **liveness check for credentials**, the credential twin of `td:reconcile` [11]'s overlay-GUID probe: attempt `/connect/token` for every declared login and fail on `login_failed`. Note the param is **`storeId` (camelCase)** — `store_id` is silently ignored and every account then fails with `user_cannot_login_in_store`, which looks like a permission problem (see `scripts/lib/graphql-auth.ts` `acquireToken`). Repairing the drift itself is a decision, not a lint: force-reconciling ~60 shared accounts to the declared values rewrites fixtures other people may be using by hand |

## Open — 2026-08-07 (found during the Sprint 26-15 frontend regression run `REG-2026-08-07-1446`)

| # | Finding | Why it mattered | Fix |
|---|---|---|---|
| B-21 | **Per-slot browser isolation is entirely unwired.** `test-data/users/agent-user-pool.csv` binds all three lanes to `{{AGENT_SLOT1_PASSWORD}}` / `{{AGENT_SLOT2_PASSWORD}}` / `{{AGENT_SLOT3_PASSWORD}}`, but those variables exist in **no env file at all** — not `.env.local`, not `.env.vcst`, not `.env.playwright.local`. The three accounts `qa-agent-slot{1,2,3}@virtocommerce.com` **do exist on vcst-qa and are unlocked** (`GET /api/platform/security/users/<userName>` → 200, `lockoutEnd: none`); only the passwords are missing | The entire mechanism that stops parallel runners colliding on login state is a no-op, so every concurrent suite falls back to the one shared account. Suite `028` detected it live mid-run — the account name flipped to "SmokeTest Runner", with browse history and an order the run never placed — and correctly left **18 of 52 cases BLOCKED** rather than misattribute contamination artifacts as product bugs. It fails in the worst direction: the pool CSV *looks* configured, and a contaminated pass reads as a real result | Populate the three passwords in `.env.playwright.local` (browser lane) and `.env.local` (Node/API lane). Then add a **preflight assert** to `/qa-regression` Step 0: for every `{{VAR}}` password token the selected suites reference, fail the run at dispatch if the name is absent from the `--secrets` file. A lane that cannot isolate its session should refuse to start, not silently share an account |
| B-22 | **`.env.local` carried three vcptcore *identities* that clobbered the vcst ones.** `ORG_USER_EMAIL`, `MULTI_ORG_USER_EMAIL`, `EUR_USER_EMAIL` were set to `agent-test-*-vcpt@test-agent.com` in `.env.local`, which loads **after** `.env.vcst`, so they overrode the correct vcst values. Verified: all three vcptcore accounts are **ABSENT** on vcst-qa while the `.env.vcst` values (`test-emily.johnson-20260310@…`, `ricreyacrouyi-3425@yopmail.com`, `larch9900.mango@mail.com`) all **EXIST** — hence the 400 on `/connect/token` that suite `042` flagged. Commented out this session (backup `.env.local.bak-pre-vcst-fix`) | Blocked SMK-005/020/021/022 outright and would have blocked most of 006/007/008/011b/032/033. `user-roles.mjs` already declares the contract — **identity from `.env.{ENV}`, secrets from `.env.local`** — but nothing enforces it, so a single stale line in a per-machine file silently retargets every org-scoped case at another environment's accounts. The failure surfaces as an auth error, which reads like an environment outage rather than a config leak | Add a `td:validate` rule (new `DV-0xx`): fail if `.env.local` declares any **identity** key (`*_EMAIL`, `*_USERNAME`, `*_ID`) — those belong in `.env.<env>`. `.env.local` should be secrets-only. Cheap, deterministic, and it closes a class rather than this instance |
| B-23 | **`.env.playwright.local` registers only 3 secret names** (`ORG_USER_PASSWORD`, `SR_REP_PASSWORD`, `TEST_USER_PASSWORD_VCST`) while the suites reference many more — `DEFAULT_TEST_PASSWORD`, `LOYALTY_VIP_USER_PASSWORD`, `MULTI_ORG_USER_PASSWORD`, `AGENT_SLOT{1,2,3}_PASSWORD`, `B2B_USER_PASSWORD` | Every unregistered name is a silent BLOCK (cost `028` five more cases: CART-052/053/071/072/073), and worse, the Playwright MCP substitutes only on an **exact** name match — so typing an unregistered name enters the **literal string** into the password field, which counts as a failed login and can lock a shared account out. Combined with "`--secrets` is read at MCP startup only", a mid-session fix is not merely ineffective, it is actively harmful | Same preflight as B-21, plus document the startup-only constraint at the top of `.env.playwright.local` itself. Consider having the runner refuse to type any `{{VAR}}`-shaped literal — if substitution did not happen, that is a configuration failure, never a value to send |
| B-24 | **The vcst `ORG_USER` account is contested, so its password is indeterminate.** `td:validate:credentials` warns that `test-emily.johnson-20260310@test-agent.com` is declared with **two different passwords** on `.env.vcst` — `b2b/users.csv [USR-006] → B2B_USER_PASSWORD` vs `user-roles.mjs [ORG_USER] → ORG_USER_PASSWORD` — and password reconciliation is therefore **disabled** for it by `user-provision.mjs` | B-22's fix restores the correct *identity*, but this account may still not authenticate with `ORG_USER_PASSWORD`, because whichever seeder touched it last set the other password. The guard is working as designed (it refuses to let two seeders fight), but the consequence is an account that no declared credential reliably opens. 12 of the 21 credential warnings are this same shape across other env layers | Give one of the two fixtures a **dedicated account** so each has a single declared password — the guard's own recommended remedy. Until then, treat `ORG_USER` on vcst as unreliable and prefer `@td()`-resolved B2B fixtures for org-scoped cases |

## Open — 2026-08-13 (found during the VCST-5588 `/qa-verify-fix` run)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-30 | **`seed:sales-rep-stats` cannot seed its active-cart fixtures on vcst-qa: `discoverStoreProducts({cartable:true})` ignores the product's `minQuantity`.** The ACME cart spec asks for `quantity: 5`, discovery hands it `0000385` (Fanta, MOQ **10**), and `addItem` rejects the line — `PRODUCT_MIN_QTY: quantity 5 is less than minimum 10` → `cart AGENT-TEST-SR-CART-ACME created EMPTY (itemsCount=0) — NOT an active cart per BL-SR-006`. The seeder then still writes the alias back, so `SR_STATS_CART_ACME` resolves to an id whose cart holds nothing. The `cartable` filter checks only `availabilityData.isInStock` (`seed-sales-rep-stats.mjs:133`), never `minQuantity`/`packSize` | Every active-cart statistic (BL-SR-006, the `salesRepCustomerCartStatistics` widgets on both sales-rep surfaces) has **no data** on vcst-qa, so those cases read as zero-valued product behaviour rather than a missing fixture — which is exactly what happened here: verifying VCST-5588 required hand-building the cart via xAPI before the widget could be exercised at all. It fails loudly in the log but silently in the data, because the alias write-back succeeds regardless | Two parts: (a) in `discoverStoreProducts`, reject a candidate whose `minQuantity > 1` unless the spec's quantity satisfies it (or raise the spec quantity to `max(spec.quantity, minQuantity)` and honour `packSize` multiples); (b) do **not** write the alias back when `itemsCount === 0` — an alias pointing at an empty cart is worse than an absent one, since `@td()` resolves and the case proceeds. The seeder already detects the empty-cart condition and logs `WARN`; that branch should skip the write-back and exit non-zero |
| B-31 | **`seed:sales-rep-stats` created a SECOND cart under an existing fixture name instead of reusing it, and its `findCart` lookup missed the live one.** `SR_STATS_CART_ACME` (`463c6a87-…`, 2 line items, $150) is alive on vcst-qa, yet the seeder's `findCart(token, name, userId)` returned null and it went down the create path — which then failed on B-30's MOQ. A manual `addItem` with the same `cartName` likewise produced a **new** cart (`0fe1d98e-…`), so `POST /api/carts/search` now returns **two** carts named `AGENT-TEST-SR-CART-ACME` in AcmeCorp. The seeder also writes the alias back regardless of outcome | Duplicate business keys break the fixture contract the same way duplicate case ids break evidence: `@td(SR_STATS_CART_ACME.id)` names one of two carts and cart-statistics assertions silently aggregate both, so a count/quantity expectation drifts with each reseed. It also makes B-30's failure look harmless — the log says "created EMPTY" while a perfectly good cart sits next to it unused | Make `findCart` match the way the fixture is keyed (name **+ organizationId**, not name + userId — the org context is what the seeder itself stamps), so an existing fixture cart is reused and mutated rather than duplicated. Add a pre-seed assert that a fixture cart name resolves to **at most one** non-deleted cart per org, and clean up the existing duplicate. Separately: skip the alias write-back when the seeded cart ended up empty (an alias resolving to an empty cart is worse than an absent one) |
| B-32 | **`td:reconcile` [11] cannot see cart-shaped overlay aliases at all.** The liveness probe runs through a **member-only allowlist**, so `SR_STATS_CART_ACME` / `SR_STATS_CART_WEST` are never checked — a torn-down or emptied fixture cart would leave `@td()` resolving to a dead id with every gate green | Same class as B-28/B-26: the gate that exists cannot see the fixture class the cart-statistics cases depend on. Unlike a security account, a cart has a real by-id read path, so this one is cheap to close | Add a **cart** branch to [11]: `POST /api/carts/search` by id, failing when the cart is missing, `isDeleted`, or holds zero line items. Keep it separate from the member allowlist rather than widening that allowlist, which is deliberately narrow |

## Open — 2026-08-12 (found during the VCST-5590 `/ba-analyze docs` run)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-29 | **Every BA-guide screenshot is gitignored, so the customer/admin guides ship with broken images to anyone but their author.** `.gitignore:68` is a blanket `*.png`, and the only re-include is `!reports/bugs/screenshots/**` (lines 72–73). Counts on `main`: `reports/bugs/screenshots/` **20 tracked**, `reports/tickets/**/screenshots/` **9 tracked** (evidently force-added by hand), `reports/ba/**/screenshots/` **0 tracked — ever**. The 8 pre-existing `sales-rep-hub-dashboard/screenshots/doc-*.png` and the 4 in `sales-rep-view-customer-profile/` exist only on the machine that captured them, as do the 13 under `reports/tickets/Sprint26-15/VCST-5590/screenshots/` | `.claude/rules/reports.md` §5 Retention says bug evidence **and** per-ticket evidence "stay tracked", and the doc-style guide §7 makes verifying image paths a ship gate — but a `[ -f ]` check passes locally and still yields a repo full of dangling images, which is exactly what happened here. A customer guide whose every screenshot 404s for the reader is not a deliverable, and the policy text currently describes a state the `.gitignore` forbids | Extend the re-include block to the two paths the policy already declares tracked: `!reports/ba/**/screenshots/*.png` and `!reports/tickets/**/screenshots/*.png` (keeping the ephemeral `REG-*`/`TLC-*`/`COV-*`/`MONITOR-*` run folders ignored — they are matched by their own rules above). Then `git add` the orphaned BA + VCST-5590 screenshots in one sweep. Whether BA guide images belong in git at all is a team call — but policy and `.gitignore` must agree, and today they do not |

## Open — 2026-08-11 (found during the VCST-5649 `/qa-test` run)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-27 | **`npm run td:validate` is red on `main` for an unrelated reason: suite `042-smoke-tests.csv` carries an invalid `@td(ADDR_NY.*)` token.** Confirmed pre-existing by stashing the run's own diff and re-running on a clean tree — identical failure. `ADDR_NY` resolves, but `.*` is not a field selector the resolver accepts | `td:validate` is the green gate every seeding and case-authoring step is supposed to finish on (`.claude/rules/test-data.md`, `/qa-test` Step 3). While it is red for a pre-existing reason, that gate cannot distinguish "my change broke test data" from "the tree was already red", so every run has to hand-verify by stashing — and the next one may not bother | Fix the token in `042` (name the field explicitly, or drop the selector if the case only needs the alias to exist), then keep `td:validate` green so the gate is load-bearing again. If a wildcard field selector is genuinely wanted, that is a resolver feature, not a token to leave in a suite |
| B-28 | **`TEST_ENV=vcptcore npm run td:reconcile` is red on a fixture that cannot exist on that env**: `LOYALTY_VIP_USER → AGENT-TEST-vip@test.virtocommerce.com` fails `/connect/token`, because vcptcore-qa has **no loyalty module at all** (89 modules, zero loyalty). Everything sales-rep-adjacent is green on the same run, including check [11] overlay-GUID liveness (16/16 resolve) | The live per-env gate reports a permanent, unfixable failure on an env where the whole domain is absent, so its exit code carries no information there — the same "already red" erosion as B-27, one env over. It also mislabels an env-capability gap as a credential drift | Make `td:reconcile` skip a fixture whose owning module is not installed on the target env — the module list is already reachable via `/api/platform/modules`, and the reconciler already resolves per-env overlays. Failing closed on a *declared* fixture is right; failing on one whose module was never deployed is not |

## Open — 2026-08-17 (found during the `REG-2026-08-17-1030` test-data repair on vcptcore-qa)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-33 | **B-22's class recurred, and this time it hit `USER_EMAIL` + `TEST_USER_ID`.** `.env.local` pinned `USER_EMAIL=mutykovaelena@gmail.com` and `TEST_USER_ID=926b17b5-…` (a **vcptcore** contact GUID). `.env.local` loads last with `override`, so BOTH envs resolved the same shopper — and `TEST_ENV=vcst` was silently handed vcptcore's contact id instead of its own `.env.vcst:17` value. Commented out this session; per-env resolution verified restored on both envs | Exactly the failure B-22 called out and proposed a guard for — the guard was never built, so the class re-occurred on the two keys B-22 happened not to list. It also made the vcptcore drift **invisible from vcst**: the same email is a 0-org personal contact on vcst but carries 29 organizations on vcptcore, so SMK-017's personal-account precondition failed on one env only | Build B-22's proposed `DV-0xx`: fail `td:validate` when `.env.local` declares any identity key (`*_EMAIL`, `*_USERNAME`, `*_ID`). This is now two confirmed instances of the same class; a one-off comment-out will not hold |
| B-34 | **A saved Skyflow card cannot be provisioned headlessly, so SMK-034 is unseedable by the test-data path.** The scoped storefront schema exposes `skyflowCards` (read) and `deleteSkyflowCard` — but **no create/add-card mutation** — and the Skyflow module publishes **zero REST paths** in its own swagger. A card only comes into existence through the Skyflow vault iframe during a real browser checkout (suite 040a `PAY-SKY-009`). Confirmed: both candidate accounts on vcptcore-qa return `NO SAVED CARDS` | SMK-034 is a Critical daily revenue-path guard whose precondition **no seeder can satisfy** — so it is permanently BLOCKED on any freshly-provisioned env, and the block reads as a payment defect rather than a missing fixture. `/qa-seed-data` cannot close it; only a browser run can | Either (a) have the smoke suite depend on `PAY-SKY-009` having run on that env, and mark SMK-034 BLOCKED-by-dependency rather than FAIL, or (b) get a vault-token seeding path from the Skyflow module so the card can be provisioned like every other fixture. Until one exists, document SMK-034 as browser-seeded-only in the suite preconditions |
| B-35 | **`customerSalesReps` returns `totalCount: 0` on vcptcore-qa although the underlying graph is correct** — so suite `050m` SR-GQL-001/003–008 now *execute* (token acquired, HTTP 200, `errors[]` empty) but fail on data. Established live: ACME_BUYER holds an **Approved** `OrganizationMembership` in `ORG_ACME` (`105c2c4e-…`); `SR_REP_PRIMARY` (`6bf9b1d0-…`, the exact id the assertion expects) holds a `Sales Representative` membership in that same org; the caller's token is now explicitly org-scoped (`organization_id` added to the alias this session). Ruled out: missing org scope, rep membership `status: null` (set to `Approved`, no change), and a targeted `Member` reindex of the reps + org + buyer | The case fails with an **empty `errors[]`**, so it reads as "no reps serve this org" — a data gap — rather than as an auth/scoping fault. That is the silent variant of the VCST-5281 trap and it survives any `errors[]`-only assertion, which is why it was originally mis-attributed to the `store_id` resolver noise | Next step is a full `Member` index rebuild (targeted document indexing was a no-op here) and, failing that, a look at how the sales-rep module derives "serves this org" — per `reference_sales_rep_serve_membership_recipients` the serve relationship may require the rep's own emailable Contact to be added to the org, which is a different write than the role membership the seeder creates. Needs `qa-backend-expert` with the module source |

## Open — 2026-08-18 (found while verifying SR-HD-004/005 for VCST-5647)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-36 | **`buildStatisticsWindows()` computes `prevWeek` as the FULL previous week, while the storefront uses the matched day-span — so the probe reports a comparison the widgets never request.** `scripts/seed-data/sales-rep/sales-rep-stats-specs.mjs:62-63` sets `prevWeekStart = weekStart − 7d`, `prevWeekEnd = weekStart − 1ms` (Mon→Sun). The live storefront request captured on 2026-08-18 sends `weekFrom 2026-08-16T21:00Z → weekTo 2026-08-18T20:59Z` (Mon+Tue, week-to-date) against `prevWeekFrom 2026-08-09T21:00Z → prevWeekTo 2026-08-11T20:59Z` (**Mon+Tue of the previous week**) — like-for-like, exactly as the same function already does for `prevMonth` and `lastYear`. The function's own docblock claims it "mirrors the storefront's `buildStatisticsWindows()`" | It manufactures a **false positive**. On vcptcore-qa today the probe reported `weekVsPrevWeek countChangePercent = −70%` (its Mon→Sun window catches 10 orders dated Aug 16) while the dashboard correctly rendered **no** indicator, because the matched Mon+Tue span genuinely holds 0 orders and the percent is legitimately NULL. That reads as "the widget drops a non-null comparison" — the exact defect class VCST-5647 fixed — and would have been filed as a phantom regression. `validate-sales-rep-stats-data.mjs` guards this model, so the wrong contract is currently enforced | Change `prevWeek` to the matched day-span: `from = weekStart − 7d`, `to = endOfDay(now − 7d)` (mirroring the `prevMonth`/`lastYear` shape already in the function), and update the guard in `validate-sales-rep-stats-data.mjs` to assert the day-span invariant for all three axes rather than only two. Keep `pastOnlyPrevious: true` — the matched span is still entirely in the past and still un-seedable |
| B-37 | **`probe-sales-rep-statistics.mjs` was broken by VCST-5647 and returned an empty report for every section.** It still selected `lastCartDate`, removed from `CustomerCartStatisticsPeriod` by the fix, so every request 400'd with `FIELDS_ON_CORRECT_TYPE` and the probe printed `currencyCode=undefined` with no periods or comparisons — while exiting 0 | The probe is the tool used to decide whether a statistics finding is real. Failing **silently and non-zero-exit** means it answers "no data anywhere", which is indistinguishable from a genuinely empty env and invites exactly the wrong conclusion. Fixed inline this session (3 lines) because it blocked the verification in hand | Done — `lastCartDate` dropped from the selection and from both `reportPeriods` calls. Worth a follow-up: make the probe **exit non-zero when `errors[]` is non-empty** instead of printing empty sections, so a schema drift can never again read as an empty environment |

## Open — 2026-08-18 (found during the REG-2026-08-17-1030 sales-rep sweep)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-38 | **The `ACME_BUYER` lockout repair silently dropped the contacts' organization association.** After this session's `seed:b2b` repair, `test-sarah.chen-20260310@test-agent.com` (USR-002) and `test-lisa.wang-20260310@test-agent.com` both authenticated fine but resolved `contact.organizations = []` / `organizationId: null` on vcptcore-qa — although `test-data/b2b/users.csv` declares both under `org_id = ORG-001`. Sign-in additionally emitted `ApolloError: Error trying to resolve field 'organizationId'`. Restored this session by PUT-ing `organizations: ["105c2c4e-…"]` on both contacts; verified live via `me.contact.organizationId` | A repair that fixes authentication while **silently destroying the org membership** is worse than the lockout it cured: the account still signs in, so nothing looks broken, but every org-scoped assertion downstream quietly tests an org-less user. It blocked 18 buyer-facing cases in suite 090 as authored, and `td:reconcile` did not catch it — its B2B check verifies org-scoping and absence of global roles, not that the declared `org_id` is actually present | Make the user seeder reconcile `organizations` from the CSV's `org_id` on every run, not only at create time, and add a `td:reconcile` check asserting that each seeded B2B user's live `organizations` contains its declared `org_id`. That check would have caught this the moment it happened |
| B-39 | **No rep fixture on vcptcore-qa has a seeded phone**, so `customerSalesReps` returns `phones: []` for all four reps and the Phone column in suite 090 (SR-FE-MSR-004, -017) renders blank — a correct render of empty data that is indistinguishable from the failure signal those cases warn about | The column is currently unverifiable: it passes whether the UI is right or broken. `scripts/seed-data/sales-rep/set-contact-phone.mjs` already exists, so this is a gap in what the seeder is asked to do, not missing capability | Seed a phone on at least one rep fixture and have the cases assert the seeded value rather than mere presence |
| B-40 | **Suite 090's own preconditions understate what the env can do.** SR-FE-MSR-015 is marked NEEDS DATA GAP, but `{{ORG_USER_EMAIL}}` (`agent-test-org-vcpt@test-agent.com`) is a corporate member of an org served by **zero** reps — exactly the missing fixture — and the case passed when run against it. Conversely SR-FE-MSR-014 (paging) genuinely cannot run: page size is 10 and the largest buyer-visible org has 4 reps | A case parked as un-runnable stays un-run forever even after the fixture arrives, and nothing re-checks the assumption | Name that account in SR-FE-MSR-015's precondition and drop the gap marker. For SR-FE-MSR-014, seed an org with ≥11 serving reps or mark it BLOCKED-by-fixture with the concrete threshold |
| B-41 | **No secret NAME is registered for the vcptcore-qa `admin` account**, so Admin-SPA cases either type the password literally into the transcript or get BLOCKED. Suite 090 lost SR-FE-MSR-022 to this; suite 092 completed only because the runner typed `Password1` in the clear | The `--secrets` mechanism exists precisely so a plaintext password never reaches the agent context or the transcript (`.claude/rules/mcp-browsers.md`). Every Admin-SPA suite either violates that or cannot run — and the repo is public, so a literal in a transcript or evidence file is a real exposure, not a style nit | Add an `ADMIN_PASSWORD_VCPTCORE` (and per-env siblings) entry to `.env.playwright.local` from the team secret store, and have Admin-SPA cases type the NAME. Note the value differs per env — `Password1` on vcptcore-qa, `Password1!` on vcptcore-qa1/stable |

## Open — 2026-08-20 (found during the VCST-5724 `/qa-test` run)

| # | Finding | Why it matters | Fix |
|---|---|---|---|
| B-42 | **HAR capture is dead on ALL THREE browser lanes — a stray legacy FILE occupies each lane's `har/` directory path.** `config/mcp-playwright-*.config.json` correctly declare `recordHar.path = ./test-results/<lane>/har/session.har` (the 2026-07-30 fix), but `test-results/chrome/har`, `test-results/edge/har` and `test-results/firefox/har` are each a **regular JSON file**, not a directory (636 KB / 2026-08-03, 448 KB / 2026-08-04, 2.3 MB / 2026-07-28) — their mtimes tell the story: firefox 2026-07-28 predates the fix, but **chrome 2026-08-03 and edge 2026-08-04 postdate it** — consistent with the MCP servers never having been restarted after the config change (`.claude/rules/mcp-browsers.md`: a config edit needs a server restart), so the old extension-less path kept being written for days after the repo looked fixed. Playwright cannot create a directory where a file already exists, so it writes **no HAR at all**, silently. Confirmed this session: the VCST-5724 chrome-lane run produced zero HAR and network evidence had to come from `browser_network_requests` instead | This is the **same failure mode the 2026-07-30 fix was meant to close, re-armed by a leftover artifact** — and it fails silently in the direction that looks fine: the config reads correct, so a reviewer checking the config concludes HAR is enabled. `.claude/rules/reports.md` §6 makes HAR mandatory evidence ("HAR always"), and `scripts/lib/regression-triage.ts` `harPathForBrowser()` resolves to the newest concrete `*.har` — which here is nothing, so every triage issue on any lane references an unopenable path. `test-results/` is gitignored, so this is per-machine and invisible in CI/review; any other developer's checkout can be in the same state without anyone knowing | `rm test-results/{chrome,edge,firefox}/har` (or rename each to `session-legacy.har`, matching what the 2026-07-30 fix intended) and let Playwright recreate the directories. Then add a **preflight assertion** — `/qa-env-check` or `test-execution-preflight` — that each configured `recordHar.path`'s parent is a directory and not a file, and fail loudly if not. A config-only check is provably insufficient: the config has been correct since 2026-07-30 while capture was broken the whole time |

## Open — 2026-09-08 (found during the VCST-5891 `/qa-test` run on vcptcore-qa)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-46 | **B-42's stray-file blocker is GONE on this machine, yet HAR capture still produces nothing on the chrome and edge lanes — so the root cause was never only the leftover artifact.** `config/mcp-playwright-{chrome,edge}.config.json` correctly declare `recordHar.path = ./test-results/<lane>/har/session.har`, and unlike B-42's state there is now **no stray `har` file blocking the path** — but there is also no `har/` directory and no `session.har`: `test-results/chrome/` contains only `nb/` plus 2026-06 console logs, `test-results/edge/` only 2026-06 console logs. The only `session.har` in the tree is `test-results/mobile/har/session.har`, dated 2026-08-27. This run drove two lanes hard (139 + 34 tool calls across ~11 storefront surfaces and a Storybook audit) and produced zero HAR on either. The executing agent nonetheless reported "HAR at `test-results/chrome/har/session.har`" — a path that does not exist | B-42 was closed out as "remove the stray file and Playwright recreates the directory". That premise is now falsified: the obstruction is absent and capture still does not happen, so the real cause is something else — most likely the live `.mcp.json` does not point the servers at `config/mcp-playwright-*.config.json` at all (it is gitignored and per-machine, so nothing in review can catch this), or `recordHar` only flushes on browser-context close and the long-lived MCP context never closes within a run. Either way `.claude/rules/reports.md` §6 mandates "HAR always", `scripts/lib/regression-triage.ts` `harPathForBrowser()` resolves to nothing, and — the part that actually bites — **agents confidently cite a HAR path they never verified**, so a reviewer believes network evidence exists when it does not | Two things, and the second matters more than the first. (1) Diagnose which of the two causes it is: dump the effective `--config` each Playwright MCP server is started with and compare against `config/mcp-playwright-*.config.json`; if the configs are not wired, fix `templates/.mcp.json.example` + the `/project-init` `gen-mcp` step, since every machine is presumably in this state. (2) Make the claim falsifiable rather than trusted: extend B-42's proposed preflight so `/qa-env-check` / `test-execution-preflight` **asserts a fresh `*.har` actually appeared** after a probe navigation, not merely that the parent path is a directory. B-42 has now been "fixed" twice (2026-07-30 config, 2026-08-20 stray file) while capture stayed broken throughout — a config-shaped or path-shaped check is provably insufficient; only observing a written file is |
| B-47 | **No runner probe can assert a computed TYPOGRAPHY property, so a ticket whose entire subject is button typography cannot land runner-native coverage — the same wiring gap as B-43, on a different property family.** `scripts/layout/layout-runner.ts` dispatches only `PROBE:{CLS,SPACING,ALIGN,OVERFLOW,TOUCH,IMAGES,IMGDIMS}`; enumerating every tag in use across all 29 rows of `048c-layout-stability.csv` confirms the same set in practice. There is no way to express `font-size == 16px`, `font-weight == 700`, `text-transform == none` or `letter-spacing == normal` | VCST-5891 changed exactly those four properties across three button sizes, and every one of them was verified this run by hand via `getComputedStyle` in both Storybook and the live storefront — evidence that is **thrown away**, because the durable suite cannot encode it. The consequence is asymmetric and bad: the *layout consequences* (overflow, wrap, parity) are automatable, so the suite will happily guard the side effects of the change while the change itself — the four properties the ticket is about — silently reverts unnoticed. Combined with B-43 (per-size size tokens) this is now two consecutive UI-kit tickets that could not land the coverage their own subject demands | Add `case "PROBE:TYPOGRAPHY"` → a `typographyAuditSnippet(sel)` in `scripts/lib/measure-layout.ts` returning the computed `fontSize`/`fontWeight`/`textTransform`/`letterSpacing`/`lineHeight` per match, plus a `[TYPOGRAPHY]` assertion arm supporting `prop == value` comparisons and an arg grammar (e.g. `[PROBE:TYPOGRAPHY] .vc-button--size--md \| props=fontSize,fontWeight,textTransform`). Ship it together with B-43's `PROBE:SIZED` — they are the same wiring job on the same file and would jointly unblock the UI-kit backlog. **Scope it per-match, not document-wide max−min**, or it will reproduce B-44's phantom-drift class on a new probe |
| B-48 | **`{{STORYBOOK_URL}}` already ends in `/`, so the natural `{{STORYBOOK_URL}}/iframe.html?...` produces a double slash that renders an EMPTY Storybook shell while still returning HTTP 200 — a fully silent failure.** `.env.vcptcore` declares `STORYBOOK_URL=https://vcptcore-qa-storybook.govirto.com/` (trailing slash). Measured directly with `playwright-core` against the `components-molecules-vcbutton--all-sizes` story: the single-slash URL yields `document.querySelectorAll('.vc-button').length === 20` after ~12s; the **double-slash URL yields `0`** (`[class*="vc-button--size--"]` also 0, `document.title` still correct, `status 200`, zero console errors) — Storybook's router does not resolve the story off the doubled path and leaves the `sb-preparing-story` loader in place forever. Cost this run: the two new Storybook cases `LAYOUT-COMP-VCBUTTON-005/006` sat at `BLOCK  ~32000ms  page.waitForFunction: Timeout 30000ms exceeded` across two consecutive runs and were nearly reverted as "Storybook is incompatible with the layout runner"; after changing the cell to `{{STORYBOOK_URL}}iframe.html` both PASS in **4.1s and 3.9s**. Secondary observation from the same probe: `page.goto(..., {waitUntil:"networkidle"})` **never settles** on a Storybook story page (timed out at 60s), so any future authoring must not use `[WAIT] networkidle` there — the runner's own `[NAV]` correctly uses `domcontentloaded`, which works. | Two ways this bites. (1) **It is invisible**: HTTP 200, correct `<title>`, no console error, no network failure — every cheap check passes while the page is empty, so the natural conclusion is "the runner cannot drive Storybook", which would have permanently written off Storybook as a runner-native surface. (2) **It is the natural thing to write**: every other case in `048c` composes URLs as `{{FRONT_URL}}/path`, and `FRONT_URL` has NO trailing slash, so the idiom is correct everywhere except this one variable. `BACK_URL` in the same file also carries a trailing slash, so the same trap exists for any backend-URL composition. | Normalise at the resolver, not per case: strip a trailing `/` from `*_URL` values in the `{{VAR}}` substitution (`scripts/lib/test-data-resolver.ts` / the runner's `subst`), or collapse `//` after substitution — then both idioms work and no author has to know. Failing that, drop the trailing slash from `STORYBOOK_URL`/`BACK_URL` in every `.env.*` layer and add a `td:validate` check that no `*_URL` value ends in `/`. A worthwhile companion, since this failed silently: have `[WAIT] <selector> >= N` report the count it actually observed on timeout (`0 of 20`) instead of a bare Playwright timeout — that one number would have pointed straight at an empty page rather than at the runner. |

## Open — 2026-09-10 (found during the VCST-5097 `/qa-test` run on vcptcore-qa)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-49 | **`regression-orchestrator` ends its turn while its runner sub-agents are still working, and the resulting task-notification reads as completion — inviting a duplicate dispatch that then races two drivers on one browser lane.** Dispatched for `REG-2026-09-10-0130` (042/015/048c), it returned *"All three suites are now running in the background… I will wait for their completion notifications"* and ended its turn. **CORRECTION — an earlier version of this entry claimed the sub-agents were KILLED. That was wrong.** The inference came from disk state 3 minutes in: 042 and 015 had only `suite-*-resolved.csv` and no `suite-*-results.json`, while the script-driven 048c was actively writing traces. In fact both agents were alive and simply had not pre-seeded their results files yet (contrary to their brief, which requires pre-seeding all cases as `PENDING` at suite start). The orchestrator later notified again — *"048c is done… now waiting on 042 and 015"* — proving its children survived. | The real hazard is **notification semantics plus an unobservable liveness state**. A notification fires whenever the agent stops with no live children *of its own*, so a mid-flight return is indistinguishable from completion in the notification text, and absence of an output file is not evidence of death. Acting on that misreading, this session re-dispatched 042 and 015 as its own children — putting **two agents on one `playwright-chrome` lane writing one JSON file**. Both duplicates detected it and stood down without corrupting the file (042: *"observing browser-tab state I never drove… stopped rather than keep colliding"*), so the damage was contained by agent discipline, not by any guard. But suite 042 cases SMK-001..005 executed under contention and their evidence is not trustworthy. | Three parts. (1) **Do not infer liveness from disk** — `ListAgents` is the only reliable check, and it should be consulted before ever re-dispatching a suite. (2) **Make the orchestrator block** until its runners finish, or have it hand suite dispatch back to the persistent session so there is one owner; a turn-ending "I will wait" is a claim it cannot honour. (3) **Add a cheap interlock** so this cannot happen at all: a runner should refuse to start when `suite-<id>-results.json` already exists for the RUN_ID with a non-`PENDING` case, or write a `suite-<id>.lock` naming its agent id. Enforcing the existing pre-seed rule would also have prevented the misdiagnosis outright. |
| B-50 | **The `ORG_USER_EMAIL` fixture on vcptcore-qa carries `passwordExpired: true`, which silently costs an entire suite.** Confirmed via `GET /api/platform/security/users/<email>`: `agent-test-org-vcpt@test-agent.com` → `passwordExpired: true`, while `agent-test-shopper-vcpt@test-agent.com` and `agent-test-sr-primary@example.com` → `false`. The account authenticates fine (header renders the signed-in user, cart, notifications), but every `/account/*` route redirects to `/change-password?returnUrl=<target>` with no bypass, so the target page never renders. Cost this run: suite **015 Quotes = 32/32 BLOCKED**, 0% signal, in `REG-2026-09-10-0130`. | Same shape as B-38: a fixture that **signs in successfully** while being unusable downstream, so nothing looks broken until a whole suite silently produces no signal. It is worse than a lockout — a lockout fails loudly at sign-in. `td:reconcile` does not check this: its B2B checks cover org-scoping and absence of global roles, not whether a seeded account can actually reach an authenticated route. A correct runner does the right thing here (record BLOCKED rather than mutate a shared credential), so the suite is permanently un-runnable until someone repairs the account out of band. | (1) Immediate — and note the obvious route does NOT work: `PUT /api/platform/security/users` with `passwordExpired: false` returns `{succeeded:true}` + HTTP 200 and **silently ignores the field** (verified 2026-09-10: before `true`, after `true`) — the same silent-200-no-op class already recorded for this platform in the `reference_token_endpoint_org_auth_testing` memory. There is no dedicated endpoint either (full `security/users` path list checked against the live swagger). The only working route is `POST /api/platform/security/users/{userName}/resetpassword` with body `{newPassword, forcePasswordChangeOnNextSignIn: false}` (schema `ResetPasswordRequest`, exactly those two fields) — passing the password value the fixture ALREADY has keeps every other consumer working while clearing the flag. (2) Durable: have `user-provision.mjs` reconcile `passwordExpired: false` on every seeded test user on each run, not only at create time — the same "reconcile, don't just create" fix B-38 asks for on `organizations`. (3) Detection: add a `td:reconcile` check asserting each seeded storefront user can reach an authenticated route (or simply that `passwordExpired === false`), so this class fails loudly at reconcile time instead of costing a suite mid-run. Secondary, independent gap found by the same run: 22 of the 32 cases also reference quote fixtures (`{{QUOTE_WITH_ADMIN_RESPONSE}}`, `{{ACCEPTED_QUOTE}}`, `{{EXPIRED_QUOTE}}`, `{{QUOTE_WITH_SUBSTITUTION}}`, `{{QUOTE_MULTI_ROUND}}`, `{{QUOTE_REJECTED}}`, `{{ACCEPTED_QUOTE_OOS}}`, `{{ACCEPTED_QUOTE_WITH_PO}}`) that are defined in **no** env layer — so suite 015 would have been largely blocked even with a healthy account. |

## Open — 2026-09-11 (found during the VCST-5472 `/qa-test` run on vcptcore-qa)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-51 | **No runner probe can assert HORIZONTAL column alignment, so the exact invariant VCST-5472 is about cannot land runner-native coverage.** `PROBE:ALIGN` → `alignmentAuditSnippet()` in `scripts/lib/measure-layout.ts` derives only `centerDriftPx` and `heightDriftPx` from `max−min` over the matched set — i.e. **vertical** center/height drift within one horizontal group. There is no way to express the cross-group, index-paired assertion the ticket needs: `th[i].left == td[i].left` and `th[i].width == td[i].width` for every column `i`. The runner's assertion vocabulary confirms it — the only `[ALIGN]` arm is `centerDrift`. | VCST-5472's whole subject is a header/body **column** misalignment (a custom `#header` dropping the leading selection cell shifts every column by one while body rows keep theirs). That is precisely a horizontal, index-paired property. It was verified this run by hand via `getBoundingClientRect` (header `th` 48px/left 17 matching the body `td`), and that evidence is **thrown away** because the durable suite cannot encode it. Note the asymmetry that makes this dangerous: suite `048c`'s two existing VcTable cases both PASS on the fixed build and would **also pass on the broken one** — `offGrid` and `heightDrift` are blind to a column being shifted sideways, so the suite reports green over exactly the defect it appears to cover. This is the third consecutive UI-kit ticket (after B-43 per-size tokens and B-47 typography) whose own subject cannot be automated, and they are all the same wiring job on the same file. | Add `case "PROBE:COLALIGN"` → a `columnAlignmentAuditSnippet(tableSelector)` in `scripts/lib/measure-layout.ts` that walks `:scope > thead > tr` and `:scope > tbody > tr`, sums `colSpan` per row, and returns per-column `leftDriftPx` / `widthDriftPx` between the header cell and each body cell at the same column index, plus `headerCells` / `bodyCells` counts. Assertion arm `[COLALIGN] leftDrift <= 1`, `widthDrift <= 1`, `cellCountMismatch == 0`. **Scope it per column index, not document-wide max−min**, or it reproduces B-44's phantom-drift class. Ship with B-43's `PROBE:SIZED` and B-47's `PROBE:TYPOGRAPHY` — one file, one dispatch switch, and jointly they unblock the whole UI-kit backlog. Cite BL-UI-005, which already claims "table-row cells share height per row" but has no horizontal counterpart. |

## Open — 2026-09-15 (found during the VCST-5890 `/qa-test` run on vcptcore-qa)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-52 | **CORRECTION to B-46: the stray extensionless `har` FILES were never gone — they were present on three of four lanes, and they are why HAR capture produced nothing.** Measured 2026-09-15: `test-results/chrome/har` (624K, dated **Aug 3**), `test-results/edge/har` (440K, **Aug 4**), `test-results/firefox/har` (2.3M, **Jul 28**) were each a regular FILE containing real Playwright HAR JSON (`{"log":{"version":"1.2","creator":{"name":"Playwright"...`), sitting exactly where `recordHar.path = ./test-results/<lane>/har/session.har` needs a DIRECTORY. `test-results/mobile/har/` was the only correct directory — and it is the only lane that ever produced a `session.har` (2026-08-27), which is consistent and was the clue. All three file dates predate B-46 (2026-09-08), so B-46's premise — *"unlike B-42's state there is now no stray `har` file blocking the path"* — was factually wrong, and its conclusion that "the real cause is something else" sent the investigation toward `.mcp.json` wiring on a false basis. Likely cause of the misreading: an extensionless file named `har` is invisible to a `*.har` glob and reads as a directory in casual `ls` output. **Actioned this run:** each stray renamed to `test-results/<lane>/session-legacy.har` (preserved per `.claude/rules/reports.md` §6 convention, not deleted) and the `har/` directories created. `test-results/` is gitignored, so this is local-machine state only. | B-42 has now been "fixed" three times (2026-07-30 config, 2026-08-20 stray file, this entry) while capture stayed broken — and the second of those was closed out against an observation that did not hold. Meanwhile `.claude/rules/reports.md` §6 mandates "HAR always", `scripts/lib/regression-triage.ts` `harPathForBrowser()` resolves to nothing, and agents keep citing `test-results/<lane>/har/session.har` in reports as though it exists. Three agents did exactly that during this run. | **Do NOT close this as fixed — the fix is UNVERIFIED.** Removing the blocker is necessary but not proven sufficient: B-46's second hypothesis (the live per-machine `.mcp.json` may not pass `--config config/mcp-playwright-*.config.json` at all, or `recordHar` may only flush on browser-context close, which a long-lived MCP context never does) is untested and still stands. Next Playwright MCP restart, drive one navigation and assert a fresh `session.har` actually appears. Durable fix is unchanged and is the one that matters: extend `/qa-env-check` / `test-execution-preflight` to assert **a HAR file was written after a probe navigation** — not that the path is a directory, and not that the config declares it. Two config-shaped fixes and one path-shaped fix have now all failed to produce evidence; only observing a written file distinguishes working from broken. Companion guard worth adding: fail the check if `test-results/<lane>/har` exists as a FILE, since that exact state has now recurred twice. |

## Open — 2026-09-16 (found during the VCST-5924 `/qa-test` pre-flight on vcptcore-qa1)

| # | Finding | Impact | Suggested fix |
|---|---|---|---|
| B-53 | **`npm run suites:lint` FAILS corpus-wide on an orphan that has nothing to do with the run: `regression/suites/Backend/page-builder/059b-page-builder-shared-components.csv` exists on disk with no `config/test-suites.json` entry.** The file is untracked (`??` in `git status`), so it is somebody's in-flight page-builder work that was left in the tree. `suites:lint` is `sync-test-suites.ts --check` and it is all-or-nothing: one unregistered CSV takes the whole manifest gate down. | Same shape as the measured mid-write-CSV outage recorded in `.claude/rules/regression.md` §Suite inventory — **one author's local state blocks every other author's gate**, tree-wide. Concretely it blocks `/qa-test` Step 3's `A` append and the `3-cases` gate for any ticket in any domain, because both re-lint the corpus. It also cannot be caught in review: the offending file is untracked, so no PR contains it and no CI run sees it. | Two parts. (1) Whoever owns the page-builder suite: register it in the manifest (plus a `selections` reference) or delete it — the lint message already states both options. (2) Durable: `suites:lint` should distinguish an **untracked** orphan from a **tracked** one. A tracked CSV missing a manifest entry is a real corpus defect and must stay a hard FAIL; an untracked one is local scratch state and should be a loud WARN naming the file, so one developer's work-in-progress cannot gate every other session's corpus writes. `git ls-files --error-unmatch <path>` is the whole test. |
| B-54 | **`test-data/aliases.vcptcore_qa1.json` does not exist, so `npm run td:validate` is red corpus-wide on `vcptcore_qa1` and every configurable-products suite is unrunnable there.** Overlays exist for `vcst`, `vcptcore`, `vcptcore_regression` and `vcptcore_stable` — but not for `qa1`, even though `.env.vcptcore_qa1` is a committed, fully-configured environment that `env:check` passes. Measured: `TEST_ENV=vcst td:validate` → exit 0; `vcptcore` → exit 1; `vcptcore_qa1` → exit 1 with 18 suites FAIL, e.g. `072b-file-text-section-cases.csv: 33/40 resolved`, failing on `@td(CFG_023_SECTIONS.Text)` / `@td(CFG_017_SECTIONS.Text)` — inline aliases whose configuration-section GUIDs live only in the per-env overlay. | `/qa-test`'s `3-exec` gate requires `td:validate` GREEN, so **no `/qa-test` FULL run can clear its own data gate on qa1**, and `/qa-regression` on qa1 cannot run the 7 configurable-products suites at all. The failure is also misleading in the expensive direction: it reads as "the test corpus is broken" rather than "this environment was never seeded into the alias registry". Note the fixtures themselves ARE present on qa1 — 16 configurable products incl. 4 with File sections were found live via `productConfiguration` — so this is purely a missing writeback, not missing data. | Run the configurable-products seeder against qa1 so the seed writes `test-data/aliases.vcptcore_qa1.json` (the writeback model in `.claude/knowledge/execution/test-data-authoring.md` §Seed writeback), or generate the overlay from the products already live there. Durable companion: `td:validate` should say **"no alias overlay exists for TEST_ENV=<env>"** as a single leading diagnostic when `aliases.<env>.json` is absent, instead of emitting N per-suite resolution failures that each look like an independent corpus defect. |
