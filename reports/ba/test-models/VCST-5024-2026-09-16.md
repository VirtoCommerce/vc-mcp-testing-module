TEST MODEL — VCST-5024 (AMENDMENT to VCST-5024-2026-09-11.md)

Ticket:      VCST-5024 | Type: Story | Status role: testable | Flow: feature-test | Priority: P2 (Medium) | Path: FULL
Mode:        --iterate --max-rounds 2 | Environment: TEST_ENV=localhost (ENV_RISK=test)
Base model:  reports/ba/test-models/VCST-5024-2026-09-11.md — AMENDED, NOT REPLACED.
             Part 0 (value chain L1–L9), Part 0r (role scenarios BSC-1..3), the condition space, the 480 → 22
             reduction and all 22 scenario rows stand unchanged unless contradicted below. They are CITED here,
             never restated: a fact stated once does not drift.
Amendment basis: operator chose "amend + execute the backlog" over re-deriving 1c/1d/1e, on the ground that the
             build delta is one commit while the base model is five days old. Recorded as a STATED DEVIATION from
             FULL's prescribed context wave in summary.json.path_route, not a silent skip. 1c, 1d and 1c-map were
             therefore NOT dispatched this run; 1r was, and returned REACHABLE.

--- A1. WHAT CHANGED IN THE BUILD ---

Base model tested vc-module-loyalty PR #17 @116e902c on vcst-qa.
This run tests PR #17 @973e7c99 on localhost — ONE commit ahead, pushed 2026-09-16T09:39Z:

  973e7c9  "fix: fix LOYALTY_INSUFFICIENT_BALANCE for organizationless carts"
  ExperienceApi/Validators/LoyaltyCartValidator.cs  +4 -9  (the ONLY file; no test added)

    -   if (store.IsOrganizationBalanceCalculationMode())
    -       balance = await loyaltyService.GetOrganizationBalanceAsync(cart.OrganizationId);
    -   else
    -       balance = await loyaltyService.GetUserBalanceAsync(cart.CustomerId);
    +   var organizationId = store.IsOrganizationBalanceCalculationMode() ? cart.OrganizationId : null;
    +   var balance = !organizationId.IsNullOrEmpty()
    +       ? await loyaltyService.GetOrganizationBalanceAsync(organizationId)
    +       : await loyaltyService.GetUserBalanceAsync(cart.CustomerId);

Deployed and CONFIRMED live: GET /api/platform/modules reports VirtoCommerce.Loyalty 3.1008.0-pr-17-973e,
which is PR #17's head oid exactly. The split REST routes balance/user/{userId} and
balance/organization/{organizationId} both answer 200, so the org-level code is present, not merely merged.

CONSEQUENCE FOR THE MODEL — scenario 2 changes role, and nothing else does:

  Scenario 2 ("no-org actor cannot spend") was the base model's P0 hypothesis, was CONFIRMED on 2026-09-11,
  and was filed as VCST-5953 (Critical, Highest, sub-task, still "To do" on the board). Its stated root cause —
  GetOrganizationBalanceAsync(null) returning 0 with no null guard — is EXACTLY what 973e7c9 addresses.
  So scenario 2 is no longer a hypothesis to confirm; it is a FIX TO VERIFY RED → GREEN, and it is this run's
  single highest-value question. Scenario 3 (the read path) is its pair and MUST be re-run beside it: the base
  model's own argument is that the DISAGREEMENT between 2 and 3 is the evidence, so agreement is what proves it.

  The fix ships NO regression test (tests/LoyaltyCartValidatorTests.cs is untouched by 973e7c9). Carried to 5b
  as a DoD miss, and it is why LOYORG-001 / LOYORG-E2E-006 executing in C1 is the only durable guard.

--- A2. WHAT CHANGED IN THE ENVIRONMENT ---

The base model ran on vcst-qa, a SHARED store. Every constraint it recorded came from that: the flip to
Organization mode needed operator approval, held for ~33 minutes, and was restored; that window is why the base
run could not execute its own 25 authored cases (C1a ran=false), why discovery was skipped, and why the visual
lane was SKIPPED.

localhost removes ALL THREE constraints. The store is flipped and STAYS flipped for this run's duration.
Measured this run, 2026-09-16, and this is a NEW finding the base model could not make:

  THE FLIP ITSELF IS A NO-OP ON EXISTING DATA. Ledger before: 33 rows, signed sum 128,593, every row
  organizationId = null. Ledger immediately after the flip: byte-identical — 33 rows, 128,593, all null.
  Mission progress before and after: 80 rows, 29 Completed, ownerId == userId on every row.

  So scenario 8's strand is STRUCTURAL, not an event: no migration runs in either direction, and the pre-flip
  rows are not rewritten, not re-keyed and not re-minted AT THE MOMENT OF THE FLIP. The re-mint VCST-5954
  described is a consequence of NEW activity creating new progress rows under the new owner key — which is
  what the base run's own g2_rederivation observed (zero (objectId, amount) overlap between the two
  populations). This sharpens scenario 8 from "the flip re-mints" to "the flip strands, and the next order
  re-mints"; it does not contradict it. VCST-5954 is Cancelled on the board, so this is characterization, not
  a re-file.

--- A3. WHAT CHANGED ON THE TICKET ---

  Attachments: the base model recorded "none — no gap, none exist". There is now ONE:
    VCST-5024-noorg-cannot-spend.gif (2026-09-14) — this pipeline's OWN evidence for VCST-5953, attached by
    the prior run. Opened and accounted for. It is prior art, not new input, and grounds nothing further.
  Comments: a third comment exists, 2026-09-16T12:55Z, implementer → reporter, body "done". It names no
    commit, no PR and no scope. Treated as a SIGNAL that 973e7c9 is offered for test, NOT as an oracle —
    it cannot tell us WHICH of the run's findings it addresses, and the diff says it addresses exactly one.
  Sub-tasks: VCST-5953 "To do" (Highest) — the fix is in the probed build but the board has not moved.
             VCST-5954 "Cancelled" (Highest) — dev's disposition of the adoption-time re-mint.

--- A4. WHAT THE CORPUS NOW HOLDS (this corrects the base run's own record) ---

The base run recorded its 25 authored cases as never executed, entering the corpus as Draft. That is NO LONGER
the state. Two later regression runs promoted 11 of them on 2026-09-14:

  075f-loyalty-organization-balance.csv  (19 cases): 9 Automated (REG-2026-09-14-1802), 10 Draft
  083e-loyalty-organization-balance.csv  ( 7 cases): 2 Automated (REG-2026-09-14-0928),  5 Draft

  Automated: LOYORG-002, -004, -008, -011, -012, -013, -016, -017, -018, LOYORG-E2E-001, -E2E-005
  Draft:     LOYORG-001, -003, -005, -006, -007, -009, -010, -014, -015, -019,
             LOYORG-E2E-002, -E2E-003, -E2E-004, -E2E-006, -E2E-007

  So the 15 Draft rows are this run's real authoring backlog, and LOYORG-001 + LOYORG-E2E-006 — the two that
  assert the no-org spend path — are BOTH still Draft, i.e. the fix under test has no executed guard at all.

--- A5. SCENARIO DISPOSITION FOR THIS RUN ---

Carried from the base run's close-out. "NOT TESTED" below is its own word, from ac_analysis.conditions_not_tested_named.

| # | Base-run outcome | This run |
|---|---|---|
| 2  | CONFIRMED defect → VCST-5953 | **RE-TEST RED→GREEN. The run's primary question.** |
| 3  | Read path falls back; disagreed with 2 | **RE-TEST as 2's pair — agreement is the proof** |
| 8  | CONFIRMED → VCST-5954 (Cancelled) | Characterized further (A2). No re-file |
| 4  | Rows written but unreachable user-scoped | Re-check: High draft, never filed, still open |
| 10 | NOT TESTED (authored as LOYORG-008) | **COVER — concurrent earn** |
| 11 | NOT TESTED (authored as LOYORG-009) | **COVER — concurrent redeem / overdraw** |
| 12 | NOT TESTED (matrix GAP R5/L8) | **COVER — locked member may READ; may they SPEND?** |
| 1,5,6,7,9,13..22 | executed or waived on vcst-qa | Re-run via C1 where a case exists |

The three NOT TESTED rows are the base model's own matrix GAPs (R5/L8 and the two RACE rows) and are the
charter core for 3x. They are not new scope; they are the scope the shared-store window denied.

--- A6. EXECUTION NOTES THAT ARE NOT IN THE BASE MODEL ---

  1. Storefront FREE-TEXT SEARCH does not match on product code. `AGENT-TEST-PTS-UNIT-001` returns zero
     results in the storefront search box, while the API path every backend case actually uses —
     products(filter: "code:AGENT-TEST-PTS-UNIT-001") — returns totalCount 1 with the right id, isActive and
     isBuyable true. The frontend lane must reach the PDP by id/slug or via its category
     (01a0a9dd-e645-7e8b-abdf-b825425e8952), NOT by typing the SKU into search, and NOT via /product/<sku>,
     which always 404s on this platform. This is an execution note, not a defect.
  2. LOYORG-001's TITLE contradicts its own ASSERTIONS. The title reads "…is blocked from spending points
     they hold" (the defect) while the assertions require validationErrors[LOYALTY_INSUFFICIENT_BALANCE] = 0
     and a created order (the correct behaviour). The assertions are right; the title describes the bug the
     case was written against. A triager reading only the title would mis-read a PASS as a regression.
     → REPAIR candidate at Artifact A phase 2a. Title only; do NOT touch the assertions.
  3. App Insights is UNCONFIGURED on localhost (APPINSIGHTS_APP_ID_* empty). 5a's log correlation is therefore
     SKIPPED, stated — not silently absent.
  4. MULTI_ORG_LOY_POOLS is absent from aliases.localhost.json though present on vcst; LOYORG-E2E-003 and
     -E2E-007 need it. Routed to 3a.
  5. LOY_PERSONAL_NOORG is spent down by every execution of LOYORG-001/E2E-006 and there is NO reversal path
     (BL-LOY-019), so its balance must be re-verified above cheapest_pts_price before EACH round of --iterate.
     This is the fixture's own limit, and it binds round 2 as much as round 1.

--- A7. WHAT THIS AMENDMENT DOES NOT CHANGE ---

  The value chain, the mechanism coverage matrix and its GAPs, the reverse edges (balance accrual and mission
  completion remain ABSENT IN PRODUCT — BL-LOY-019), Part 0r's role scenarios, the archetype and UIP sweeps,
  the reduction and its stated attack surface, and the docs-grounding verdict (NONE EXISTS; the feature
  CONTRADICTS two published statements — scenario 21). All stand as written on 2026-09-11.
