# BL Proposals — Loyalty Missions (VCST-5320 / VCST-5346) — 2026-08-28

Five candidates that did **not** auto-apply. Source axis is solid for all of them (`vc-module-loyalty` @ `1be73b4`); what is missing is named per row. The sixth candidate cleared the bar and was applied as **BL-LOY-015** (see `reports/knowledge/BL-AUDIT-2026-08-28.md`).

Ids are **not** assigned here — an id is a citation contract and is allocated at apply time, fresh, from the current maximum. `BL-LOY-011` is a retired/reserved gap and must never be reused.

| # | Proposed invariant | Severity | Value (business · product → label) | Verdict | Why it did not apply |
|---|---|---|---|---|---|
| 1 | Mission accrual excludes loyalty-currency (points-priced) line items — the mission analogue of `BL-LOY-009` | `P0-revenue` | high · none → **high** | UNGROUNDED | Live axis absent. `PerSkuGoal` is unambiguous in source (`:413-423`, increment `:420`, no currency predicate) but no fixture exists that puts a PTS-priced line on a PerSku target — the case that would show it (`MSN-E2E-007`) is BLOCKED ON DATA. `OrderValueGoal`'s live manifestation additionally depends on how `order.Total` composes in Mixed Cart. **Unblocks when** a PTS-priced PerSku-target fixture is seeded. |
| 2 | Mission progress and granted points reverse when the contributing order is cancelled or rejected | `P0-revenue` | high · none → **high** | UNGROUNDED | Live axis absent and it needs a **mutation** — cancelling a settled order — which this run deliberately did not perform on the shared environment. Source is decisive on the mechanism being absent: both loyalty handlers filter `EntryState.Added` only (`LoyaltyMissionHandler.cs:22`, `LoyaltyProgramHandler.cs:44`); `ModuleConstants` declares only `Earned`/`Redeemed`, so no reversal operation can even be constructed; the ledger's decrement branch exists (`Earned ? + : −`) and nothing calls it for missions. **Unblocks when** a disposable fixture order can be cancelled after settlement. |
| 3 | A mission author can gate mission eligibility on order status, as a loyalty-program author already can | `P1-data` | medium · none → **qualified, but HELD** | HELD (value) + UNGROUNDED (live) | Business `medium` promotes only at product `medium`+ (≥3 citing cases, or cross-domain reach); demand is `none` today. Source is clean: `OrderStatusCondition` is offered by `LoyaltyProgramConditionAndRewardTreePrototype` and withheld from `LoyaltyMissionConditionAndRewardTreePrototype`, so the class compiles and is unreachable from mission authoring — a wire-up gap, not a missing feature. Re-score once cases cite it. |
| 4 | A `Published` mission with `public=false` stays out of the customer-facing progress query | `P2-ux` | low · none → **low** | HELD (value) | Business `low` **never** promotes — demand cannot buy a cosmetic rule into a file whose purpose is judging PASS/FAIL. Independently, the product intent is unresolved: the open bug records three plausible readings (visibility gate / publishing hint / vestigial), so no direction can be asserted yet. Needs a product decision before it is an invariant at all. |
| 5 | A mission grants its reward at most once per period, and a given order contributes to a given mission at most once | `P0-revenue` | high · none → **high** | UNGROUNDED | **This is the positive oracle — the one candidate the implementation SATISFIES.** Source is decisive (`:270-273` Completed short-circuit; `:311` + `:488-497` dedup on `(missionId, orderId, userId)`; atomic save `:322-324`). Live axis could not be gathered from this session: the account with settled mission grants was not reachable — `LOYALTY_VIP_USER_EMAIL` / `_PASSWORD` resolve empty in this env layer, and the admin account carries zero `Earned` ledger entries. **Unblocks with** any account holding a settled mission reward. |

## Why the live axis matters here rather than being waived

`bl-audit-criteria.md` §1a lets the **Docs** axis be marked `N/A` for a project-specific extension — and Loyalty Missions qualifies (no VirtoOZ surface, checked and absent). It explicitly does **not** let `N/A` cover a missing Source or Live axis: *"When in doubt, UNGROUNDED, not N/A."* Four of these five have complete source evidence and would read as confirmable to a reader who skipped that clause. They are held anyway, because an invariant asserted from source alone would be a claim about what the code says, not about what the system does — and the whole point of the three axes is that those differ.

## Notes for whoever picks these up

- **A source-only reading nearly went wrong once already this run.** The exploratory session's finding #5 framed the missing order-status gate as missions-specific; re-reading both handlers showed the `EntryState.Added` filter is **module-wide**, shared with the program path. The interesting asymmetry is the condition-tree wire-up (#3), not the runtime filter. Cite the corrected form.
- **Candidate 5 is worth filing even though it passes.** An oracle file made only of things the product gets wrong is one nobody trusts, and a positive invariant is what lets a case assert a guarantee rather than probe for a defect.
- **Coverage:** `BL-LOY-015` currently reports `BLC-004` (no citing case). That is a coverage gap for `/qa-test-lifecycle` Phase 3, not a defect in the entry, and it must not be closed by fabricating a case here.
