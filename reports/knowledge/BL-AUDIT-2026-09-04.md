# BL-AUDIT-2026-09-04

**Scope:** `BL-SR-002` (single entry). **Env:** vcst-qa · SalesRep `3.1007.0-pr-14-5569`, Xapi
`3.1021.0-pr-84-0180`, platform `3.1063.0`. **Mode:** apply.

**Why this entry, and why only this entry.** `npm run oracles:rank --axis=bl` puts `BL-SR-002` **second
in the whole queue** — `high`/`high`, T1, score 80, **54 citing cases** — and the concurrent
`/qa-test VCST-5733` run had just produced live + source evidence that its creator half is false on a new
surface. The remaining scope was **not** audited: 215 other invariants and 50 dangling-citation clusters
need a live axis, and a `/qa-test` C1 regression (`REG-2026-09-04-1015`) was holding a browser slot, so
running 3 triangulation agents would have breached the 3-slot cap and contaminated shared sales-rep
fixtures (the measured `075d`/`083d` collision). **This entry needed no browser at all** — see below.

## Verdict table

| ID | Verdict | Value | Docs | Source | Live |
|---|---|---|---|---|---|
| `BL-SR-002` | **DRIFT** → applied | `high · high → high` (T1, sc 80, 54 cases) | README@`dev` L459 / L489 | `SalesRepOrdersQueryHandler` blob `8f7253d0` | this session, browserless GraphQL |

**Three axes, all present, all agreeing — and none required a browser.** The live axis was two direct
`/graphql/sales-rep` probes on one rep token in one window, so the concurrent regression's browser slot was
never contested and no fixture was touched.

- **Docs** — the README scopes the creator rule *narrowly*: *"**All statistics and rankings** obey the same
  data-isolation rule … they count only the data the calling rep created"* (L459) and *"**Every statistics
  query** is scoped two ways"* (L489). And `salesRepCustomerOrders` appears **0 times** in it — the surface
  arrives with the unmerged PR #14, so `dev`'s README predates it. The silence is *consistent* with the
  narrowing, not contradictory to it.
- **Source** — `SalesRepOrdersQueryHandler.BuildSearchCriteria` sets **both** `criteria.OrganizationIds`
  **and** `criteria.CustomerId = request.UserId`. The customer-orders handler applies organization scope
  only.
- **Live** — same rep, same store, same token, same window: `salesRepOrders` → **24** orders;
  `salesRepCustomerOrders` → **94** across **29 distinct `customerId` values**. A creator-scoped surface
  returns exactly one distinct customer.

## Applied — one entry, body only

`BL-SR-002`'s Rule was *"Every sales-rep figure (orders, carts, counts, top sellers, customer list) counts
only the carts/orders the calling rep created…"*. It is now split into a **universal membership half (a)**
and a **statistics-and-rankings-only creator half (b)**, with an explicit carve-out naming
`salesRepCustomerOrders` / `salesRepCustomerOrder` as **deliberately creator-agnostic**. `Verify` and
`Violation signal` were re-cut per half, `Source` re-anchored, and an `Amended:` stamp added.

Three things deliberately **not** done:

- **The ID is unchanged.** IDs are a citation contract and 54 cases point at this one; renumbering would
  silently repoint every one of them.
- **No second invariant was created** for the customer-orders family. That would be a **MISSING** verdict,
  which must clear the significance gate *and* have a docs axis — and the docs axis for that surface does
  not exist yet (PR #14 unmerged). Narrowing the existing entry is the smaller, fully-grounded edit.
- **The Severity-Tags meta table was not touched** (body-only rule), and the entry stays env-agnostic.

**The amendment makes the existing citations true rather than requiring a remap.** Before it, the 6 `097`
and 6 `050m` customer-orders-family rows cited a rule that was half-false for them; now they cite one that
carves them out correctly.

## Gate — before / after

`npm run bl:lint` → **blocking: 0**. Findings unchanged: `BLC-002` 50 · `BLC-004` 19 · `BLL-005` 4;
**216** invariants. Unchanged is the *correct* signature for a body-only DRIFT — no citation moved, no
entry added, nothing renumbered.

## Citation reconciliation (Step 4) — handed off, not written here

This skill never edits a CSV. Two hand-offs, both from the `/qa-test VCST-5733` disposition of all 38
`BL-SR-002`-citing rows (37 CONFIRMED / 1 RE-BASE):

- **`SR-CO-012` → RE-BASE**, owned by `/qa-test` 5a, rewritten from that run's own evidence. It asserts a
  serving rep is *refused* on a typed buyer-page URL; the assignee stated on 2026-09-03 that membership
  grants full buyer rights **by design** (inherited from the "LEO" client project).
- **7 incidental citations → `/qa-review-tests --fix`** (Dimension 6): `SR-GQL-130` really tests
  `BL-SR-011`; `SR-FE-051` / `SR-CP-054` / `SR-HD-053` test `BL-SR-004` zero-value formatting; `SR-CO-010`
  tests action gating (`PROPOSED-BL-SR-036`); `SR-CO-018` tests the route guard; `SR-CP-052` tests cache
  freshness. Worth doing rather than shrugging at — seven loose citations on a `[P0-security]` invariant
  inflate its measured **product value**, which is the input `oracles:rank` scores when deciding what may
  enter the oracle at all.

## Not applied / outstanding

- **215 invariants + 50 dangling clusters unaudited** — needs the live axis and therefore browser slots.
  The `BLC-002` clusters are the highest-value next scope (`BL-PAY-005`, `BL-PAY-002`, `BL-L10N-004` are
  each cited by several cases that resolve to nothing — false traceability), and per the ADD-or-REMAP rule
  a cluster of that size usually means the oracle is **missing content authors expected to find**.
- **No proposals file written** — this run produced no CONTRADICTORY / UNGROUNDED / RETIRE verdict.

## One process note worth recording

The first attempt at this edit was made with `node -e` inside a double-quoted bash string. Every
backtick-quoted identifier was command-substituted away, and mangled text (`", the three statistics
queries ( / / )"`) briefly landed in a `[P0-security]` entry. It was caught immediately, restored
byte-identical from a pre-edit copy (`git diff --stat` empty), and re-applied from a **script file** with
anchor-uniqueness checks plus explicit mangling-signature and required-identifier guards that abort
without writing. Recorded because the oracle is read by every downstream skill as ground truth: a silent
mangle here would have been judged against for weeks.

## Correction to this report's own Step-4 hand-off (added 2026-09-04, after inspection)

The "7 incidental citations → `/qa-review-tests --fix`" hand-off above is **withdrawn as a remedy.** It was
right that BL-SR-002 is not the *governing* invariant for those rows; it was wrong about what to do.

Inspection of the actual cells shows **all four inspectable cases already cite the governing invariant
alongside it** — `SR-FE-051` / `SR-CP-054` / `SR-HD-053` carry `"BL-SR-002, BL-SR-004"`, and `SR-CP-052`
carries `"PROPOSED-BL-SR-FRESHNESS-001, BL-SR-002"`. A multi-citation cell is this corpus's normal way of
saying *governed primarily by X, also constrained by Y*; it is not a mis-citation. (`PROPOSED-*` is an
established convention, not a dangling ref — `bl:lint` exempts it, and 0 of the 50 BLC-002 findings involve
one.)

And **`SR-CP-052`'s BL-SR-002 citation is outright correct**: its deciding assertion is *"org_a's re-visit …
never org_b's or org_c's data"*, which IS the membership half. Calling it incidental was the error.

Stripping those citations would have destroyed real traceability and reduced a `[P0-security]` invariant's
measured product-value demand — to fix something that is not a defect. **No CSV change is warranted.** The
observation survives only as a note: BL-SR-002 is cited more broadly than it governs, which is worth
knowing when reading its 54-case demand figure, and is *not* worth editing 7 rows over.

The `SR-CO-012` **RE-BASE** hand-off is unaffected and still stands — that one is owned by `/qa-test` 5a,
rewritten from the run's own evidence, and deliberately not pre-repaired.
