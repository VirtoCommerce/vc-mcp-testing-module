# BL Audit — 2026-07-24 (Sales Rep, scoped to TLC-2026-07-24-1906)

**Trigger:** `/qa-test-lifecycle module Sales Rep + frontend Sales Rep feature` → Phase 4c, scoped to the 3 BL candidates this run surfaced (2 new + 1 flagged contradiction). Not a whole-domain sweep.

**Auto-apply this run: 3 (BL-SREP-002, BL-SREP-003, BL-SREP-001).** BL-SREP-001 was promoted **on source authority, operator-directed** — a deliberate deviation from the strict "applicable-axes contradictory → hold" rule: source (merged PR #2391) confirms the invariant, docs are waived (pre-GA), and live shows the pre-fix redirect ONLY because the fix is not on the pinned vcst-qa artifact (deploy lag, not a real contradiction). The oracle entry carries an explicit live-verification-pending-deploy caveat so the redirect isn't misclassified as a FAIL; re-verify redirect→reachable once PR #2391 deploys. Evidence-bar policy = **applicable-axes**: Sales Rep is a **brand-new, undocumented module** (`SalesRep 3.1000.0-pr-4-6596`, PR prerelease) → the docs axis is structurally unavailable, so **docs is waived (N/A)**, not a permanent blocker. A candidate promotes when the axes that CAN be verified (**live + source**) are **both met and agree**; it stays a draft when an applicable axis is missing or contradicts. Under that bar: **SREP-002 promoted** (live + source CONFIRM); **SREP-003 promoted** after its blocker was cleared — a `customer:read`-only fixture was seeded (`seed:rbac`) and the SR-ADM-023 three-account RBAC verification CONFIRMED the gate live (source = controller `[Authorize]` map); **SREP-001 held** (live contradicts source — deploy lag). Draft SREP-001 in `reports/ba/bl-proposals-2026-07-24.md` with a re-audit trigger.

## Axes used
- **Docs:** VirtoOZ / Context7 — **absent** for Sales Rep (Context7 returned only adjacent Profile/Org/Order xAPI). New module, not yet documented.
- **Live:** vcst-qa, build `SalesRep 3.1000.0-pr-4-6596` / Platform 3.1048.0 / theme `2.54.0-pr-2395` — `qa-testing-expert` on firefox/edge (TLC Phase 5).
- **Source:** GitHub — vc-frontend PR #2391 (VCST-5494), vc-module-sales-rep #2/#3, refreshed `graphql-schema.md`.

## Verdict table

| Candidate | Docs | Live | Source | Verdict | Applied? |
|-----------|:----:|:----:|:------:|---------|:--------:|
| PROPOSED-BL-SREP-001 — hub reachable w/o own org membership | N/A (waived) | pending deploy (build lacks fix) | **CONFIRMS** (PR #2391) | **PROMOTED — source authority, operator-directed; live-verify pending** | **Yes → BL-SREP-001** |
| PROPOSED-BL-SREP-002 — Dashboard stats scoped to served orgs | N/A (waived) | **CONFIRMS** | **CONFIRMS** | **PROMOTED — 2 applicable axes met** | **Yes → BL-SREP-002** |
| PROPOSED-BL-SREP-003 — embedded Admin app permission-gated | N/A (waived) | **CONFIRMS** (SR-ADM-023 RBAC) | **CONFIRMS** (controller `[Authorize]`) | **PROMOTED — blocker cleared, 2 axes met** | **Yes → BL-SREP-003** |

## Key reconciliation (SREP-001)
Source and live disagree **only because of deployment lag**, not a real behavioral contradiction:
- PR #2391 (fix: clear `requiresOrganization` on rep-facing routes) merged to `dev` 2026-07-23.
- vcst-qa is pinned to theme **`2.54.0-pr-2395`** — a different PR-branch build that does **not** contain #2391. The deploy PR `vc-deploy-dev#6164` carrying #2391's artifact is still **open/unmerged**.
- ∴ live correctly shows the **pre-fix redirect** for the deployed artifact. The *intended* behavior (per merged source) is **reachable**. Do not canonicalize until the fix deploys.
- Consequence: suite **089 SR-FE-013/021** encode the redirect as intended — **STALE against merged intent** but still pass on the current pinned build (STALE-PENDING-DEPLOY).

## Applied to business-logic.md
- **BL-SREP-001** — rep-facing hub pages reachable without the rep's own org membership `[P1-data]` — promoted on SOURCE authority (merged PR #2391), operator-directed; **live-verification pending the VCST-5494 deploy** (caveat stamped in the entry).
- **BL-SREP-002** — Sales Rep dashboard statistics scoped to the rep's served organizations `[P1-data]`.
- **BL-SREP-003** — embedded Sales Rep Admin app gates on customer-member + platform-security permissions (full controller `[Authorize]` matrix incl. the account-ops class), not on `sales-rep:access` `[P1-data]`.
- All three under "Domain 20: Sales Rep (BL-SREP)". Body-only per policy (`feedback_bl_promotion_table_separately`); the Invariant Coverage Summary table + frontmatter count were synced to **148** (deliberate follow-up edit, not auto-applied).

## Routed to proposals (`reports/ba/bl-proposals-2026-07-24.md`)
- PROPOSED-BL-SREP-001 / -002 / -003 (all draft, docs-pending, per-candidate re-audit triggers)
- Two stale test-case items: 089 SR-FE-013/021 (redirect-as-intended) and 050m SR-GQL-033/034 + 091 SR-CP-009/010/011 (`salesRepOrderStatuses` → `salesRepOrderFilterRules`/`salesRepOrders(filter:)`, live-confirmed rename).

## Re-audit triggers
Promote any SREP-* invariant only when **all** hold: (1) VirtoOZ Sales Rep docs published, (2) module on a **stable** release (not `-pr-*`), (3) the deployed vcst-qa artifact confirmed to include PR #2391, (4) for SREP-002 a cross-org negative test exists, (5) for SREP-003 a restricted-admin fixture exists. Coverage reconciliation: `BL-B2B-012` remains the next free B2B id; `BL-SREP-*` is a proposed new domain (0 canonical entries today).
