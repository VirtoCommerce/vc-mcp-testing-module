# BL-AUDIT-2026-07-30 — `/qa-review-bl` Phase 4c, single candidate (VCST-5618)

Scope: exactly one candidate, `PROPOSED-BL-AUTH-014` (source ticket VCST-5618, PR `VirtoCommerce/vc-platform#3093`).

## Verdict

| Axis | Result |
|---|---|
| Docs | Waived — VirtoOZ `PlatformDeveloperGuide` query ("401 vs 302 cookie auth API") returned only the general mixed-cookie+JWT model and OIDC/Azure-AD pages; no coverage of this status-code contract. Structurally unavailable, not skipped. |
| Source | ✓ CONFIRMS — `ApiCookieRedirectHandler.cs` `IsApiRequest()`: `Path.StartsWithSegments("/api", OrdinalIgnoreCase)` OR `X-Requested-With: XMLHttpRequest`; `Startup.cs` wires `OnRedirectToLogin`→401, `OnRedirectToAccessDenied`→403 on `options.Events` (assigned, not replaced, preserving `OnValidatePrincipal`). xUnit `ApiCookieRedirectHandlerTests.cs` covers all branches incl. `/apiary` non-match and `/connect/authorize` still-302. |
| Live | ✓ CONFIRMS (independent re-check via qa-testing-expert, own Firefox session, real navigation + network inspection, on the PR's own deployed build) — unauth `GET /api/platform/settings/...` → 401, `content-length:0`, no `Location` header. `/apiary/...` → 404 (not 401), consistent with segment matching. **Correction to the candidate's negative-control claim:** the Admin SPA root `/` is served anonymously (200, client-side hash-route login) — it never triggers the cookie challenge at all, so "non-API browser navigation redirects (302)" does not hold generically. The real, source-confirmed redirect-preserving case is the OIDC `/connect/authorize` flow (unit-test only, not independently live-checked). |

**Verdict: MISSING → CONFIRMED, applied as BL-AUTH-014.** Two of two applicable axes agree; docs validly waived.

## Applied

`knowledge/oracles/business-logic.md` — new `BL-AUTH-014` inserted after `BL-AUTH-013` (body-only, env-agnostic), rollup row `Users & Auth` bumped `13→14` total / P1 `9→10`. File has a known pre-existing whole-document duplication (per BL-AUDIT-2026-07-29) — edit applied identically to **both** copies via `replace_all`, per that report's own precedent (nothing lost on future de-dup).

## Caveat — flag to requester

PR #3093 is **open, not merged** (`merged_at: null`), contradicting the ticket description's "merged to dev." The invariant is evidenced against this PR's own deployed prerelease build only. Re-audit once merged to `dev`.

## Not applied

Nothing routed to `bl-proposals` — the single candidate in scope was confirmed.
