# BL-AUDIT-2026-09-08 — targeted re-audit: BL-LOY-015

**Scope:** `BL-LOY-015` only (single-entry re-audit, triggered by the VCST-5916 investigation this session).
Not a domain or full-oracle pass — `bl domain loyalty` / `bl all` remain unrun.
**Mode:** apply (not `--dry-run`). Single writer, no parallel fan-out — see *Method* below.

## Verdict

| Entry | Verdict | Value | Docs | Source | Live |
|---|---|---|---|---|---|
| `BL-LOY-015` | **DRIFT** | high · medium → **high** (APPLY) | `N/A` (§1a, project-specific extension — already declared, re-checked: the VirtoOZ Points History page is order-centric and states nothing about missions) | `vc-module-loyalty` @ `dev` — `DataLoaderContextAccessorExtensions.cs` → `LoadLoyaltyObject()` `objectType switch`; `Schemas/LoyaltyOperationLogType.cs` | `{OBSERVED}` 2026-09-08 — xAPI `object: null` on 69/167 rows; Platform REST returns `objectType: "LoyaltyMissionProgress"` + `objectId` for those same rows |

Docs waived under bl-audit-criteria §1a; **source + live both fresh this run and agreeing**, so the
two-axis bar is met. Gate (`oracles:rank --explain=BL-LOY-015`): `APPLY — DRIFT · business high ·
product medium — correction to an existing entry; the bar governs growth, not correction`.

**Live axis re-derivability:** the observation sits on the *persistent* `LOYALTY_VIP_USER` fixture
(`memberId` / `securityAccountId` in `aliases.vcst.json`), not a per-run disposable one, and the
recorded ledger entry ids + `objectId`s let a later auditor re-read the same rows. Deployed artifact:
`VirtoCommerce.Loyalty 3.1006.0` / `VirtoCommerce.Xapi 3.1020.0`.

## Applied

1. **Measured bullet retitled + narrowed.** *"Measured, and worse than 'no mission id' — a mission grant
   carries NO attribution at all"* → *"Measured on the xAPI surface — a mission grant projects NO
   attribution"*, with the 2026-08-28 numbers preserved as the point-in-time reading and a 2026-09-08
   re-read (69/167) appended.
2. **New NARROWED bullet.** States that the earlier claim — mission grants "attributable to nothing",
   "maximum discrimination is the amount" — was a **GraphQL-surface-only** reading and is **not true of
   the record**; gives the REST-vs-xAPI disagreement on the same entry; names the three consequences
   (cost estimate drops to one `switch` arm; an admin caller *can* attribute today; storefront
   authoring guidance unchanged).
3. **Status sharpened.** `VIOLATED by the current implementation` → violated **for the caller the Rule
   names** (storefront/xAPI), explicitly **not** violated at the persistence layer.
4. **Read-side Source anchor added** — `LoadLoyaltyObject()`'s `objectType switch` with the default arm
   quoted, plus the deployed module versions. This is the line the invariant now turns on.
5. **`Amended:` stamp** — `2026-09-08 (auto-applied, triangulated — BL-AUDIT-2026-09-08)`.

Full diff: `git diff .claude/knowledge/oracles/business-logic.md`.

## Held / Excluded / Not applied

None. No MISSING candidate was raised (no new entry, so the growth gate did not engage), and nothing
came out CONTRADICTORY / UNGROUNDED / STALE-RETIRE — so **no addition to
`reports/ba/bl-proposals-2026-09-08.md`** (that file belongs to another session's run; not touched).

## Citation reconciliation

**No remap required, and no `/qa-review-tests --fix` dispatch.** No ID was added, renumbered or
retired, so every existing `Business_Rule` citation stays true. Cited by 3 cases across 4 suites
(`075d`, `075e`, `083c`, `083d`).

Two observations handed forward rather than acted on here:

- `075d` carries justification prose quoting the old reading (*"a mission-granted ledger entry carries
  object = null (measured live 2026-08-28 …)"*). The **assertions themselves remain correct** — the xAPI
  still returns `null` — so nothing fails and no cell is wrong; the prose is merely incomplete now. A
  low-value refresh for a future `/qa-review-tests` pass, not a defect.
- `083c` **MSNF-080** is already marked `EXPECTED TO FAIL`, which stays consistent with Status remaining
  violated for the storefront caller.

## Also updated (outside the oracle)

`.claude/knowledge/api/graphql-schema.md` — added the two loyalty ledger types' **field lists**
(`type`/`orderId`/`orderNumber` are on the **nested** `object`, and selecting them top-level fails
validation with `Cannot query field 'type' on type 'LoyaltyOperationLog'` — reproduced this session),
the correct selection string, and the `object: null`-on-mission-rows contract with its REST
counterpart. The pre-existing sentence there was **correct**; it was missing the parent shape, which is
what made it misreadable. One phrase tightened: *"no per-mission attribution on the points ledger at
all"* → *"…on the points ledger GRAPHQL SURFACE"*.

## Gate

| Gate | Result |
|---|---|
| `npm run bl:lint` | **0 High** · 216 invariants parsed · 73 findings (65 Medium, 8 Informational) — all pre-existing BLC-002 dangling citations + BLL-005 sequence gaps; **`BL-LOY-015` clean** |
| `npm run context:check` | always-loaded 69,261 / 80,000 chars · longest line 2,151 / 2,500 — within budget (edits landed in the on-demand `knowledge/` tier, so no always-loaded cost) |

## Method — deviation from SKILL.md Step 1, stated

Triangulation was **not** fanned out to parallel `ba-system-analyzer` agents. Scope was a single entry
whose source and live axes had already been measured first-hand earlier in this session (live GraphQL
introspection, the 167-row xAPI read, the Platform REST cross-read, and both `vc-module-loyalty` source
files). Dispatching agents to re-derive evidence already in hand would have added cost and no
independence. The docs axis was re-checked directly against VirtoOZ. Recorded here because the skill
specifies the parallel path as default.
