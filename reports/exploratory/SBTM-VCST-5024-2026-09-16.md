# Exploratory Session: VCST-5024 Loyalty Organization Balance (`/qa-test` Step 3x)
**Date:** 2026-09-16 | **Duration:** ~45 min (hard box) | **Lane:** playwright-chrome
**Platform:** localhost (ENV_RISK=test) | **Build:** VirtoCommerce.Loyalty 3.1008.0-pr-17-973e | **Theme:** 2.58.0-pr-2475-d710-d710e94f
**Store setting (read live):** `{"storeId":"B2B-store","loyaltyEnabled":true,"loyaltyMode":"Mixed Cart","loyaltyCurrency":"PTS"}`
**Session type:** [EXP] | **Technique:** User-flow edge enumeration + surprise-seeking
**Charter:** received from the Test Model (5 sources). **Files no bugs** — triage is 5a.
**BL Refs:** BL-LOY-008, -015, -018, -019 | **Edge-Case Refs:** ECL-14.3

## The measurement that reframes the charter
`/api/loyalty-program-operation-log` (admin token), all read-only:

| Scope | Balance |
|---|---|
| Outlet org `01a0a9fa-8015-…e10` (pooled) | **0.0** |
| ORG_LOY_A / _B / _LOCKED (user-keyed) | **0.0 / 0.0 / 0.0** |
| LOY_PERSONAL_NOORG (user-keyed) | **33,872.0** |

**The org pool is empty and no member holds points.** Every "can X spend the pool" question therefore
refuses for *insufficient balance*, never for *authorization* — the two are indistinguishable on this
fixture. This is the session's central finding and it binds charter items 1, 2 and 5.

## Charter coverage
| # | Item | Status |
|---|---|---|
| 1 | R5/L8 — locked member SPEND | COVERED, **verdict UNDECIDABLE** (pool=0) |
| 2 | LOYORG-010 `{HYPOTHESIS}` → `{OBSERVED}` | COVERED — oracle **contradicted** |
| 3 | R3/L6 no-org missions | COVERED. R3/L9 Admin widget **NOT REACHED** (box expired) |
| 4 | R7/L8 stranded-balance spend | COVERED — **fix verifies GREEN** |
| 5 | AC sentence-2 restrictive half | PARTIAL — item 1 instance only; others NOT REACHED |

## `{HYPOTHESIS}` → `{OBSERVED}`
**LOYORG-010** asserted `errors[] non-empty` for a locked member reading the pooled balance + ledger.
**OBSERVED: served, no errors, empty.** HTTP 200, `errors[]` absent.
`loyaltyBalance{currentBalance:0.0, resultBalance:0.0}` · `loyaltyPointsHistory{totalCount:0, items:[]}`;
UI `Balance: 0` / "No records found" (`3x-01`). All three oracles are **contradicted** — but see the
confound: A and B would read 0 too, because the pool is 0. The lock is not shown to be the cause.

## Net-New Scenarios Discovered
| # | Scenario | Why uncovered | What we found | Oracle ref | Fate |
|---|---|---|---|---|---|
| 1 | Locked member + **non-empty** org pool attempts to spend | R5/L8 GAP; fixture cannot express it | Pool=0 makes lock vs shortfall indistinguishable — the `Contains()` hypothesis is **unfalsifiable today**. Needs a fixture that credits the org | NONE | **PROMOTE** (blocked on fixture) |
| 2 | Locked member's read is **indistinguishable** from a legitimate empty pool | LOYORG-010 asserts an error that never comes | Served-empty: same 200/0/[] either way. A revoked member sees a clean, plausible account page | ECL-14.3 | **PROMOTE** |
| 3 | No-org actor spends **stranded pre-flip** points in Organization mode | R7/L8 GAP; LOYORG-001/E2E-006 still Draft | **No `LOYALTY_INSUFFICIENT_BALANCE`.** Only `LOYALTY_ONLY_POINT_PRODUCTS_NOT_ALLOWED` remains (`3x-06`) | BL-LOY-008 | **PROMOTE** |
| 4 | No-org actor's mission surface in Organization mode | R3/L6 GAP | Renders fully: 33,872 pts, missions Completed/partial, "Redeem your points" (`3x-04`). Points-history agrees: 33,872 + full ledger (`3x-05`) | BL-LOY-018 | **PROMOTE** |
| 5 | Points-only SKU in a non-PTS display currency | No suite covers currency × loyalty-SKU | USD PDP shows **"Price: $0.00"** + no Add-to-Cart (`3x-02`); for a second actor the price block rendered blank. Reads as *free*, not *unavailable* | NONE | **BACKLOG** — overlaps trap 1; needs a product decision on Mixed Cart |
| 6 | Cart surfaces a raw infra failure to the shopper | Not a loyalty scenario; found in passing | `ws://localhost/graphql` handshake 400 → red toast *"Our server is currently experiencing technical issues"* over the header, blocking clicks | NONE | **BACKLOG** — confirm on a deployed env first |

## BL-LOY-008 — verified, clause by clause (ORG_LOY_LOCKED, `3x-03`)
Cart `addOrUpdateCartShipment.validationErrors[]` carried:
`LOYALTY_INSUFFICIENT_BALANCE` with `errorParameters: required=1, available=0`.
Code present ✓ · `required > available` ✓ · params non-empty ✓ · order NOT created (Place order `[disabled]`) ✓ ·
cart intact and readable ✓ · `available` (0) **equals** the balance the same actor is shown on
`/account/points-history` (0) ✓. **BL-LOY-008 HOLDS.**

## Oracle Feedback (proposals only — never edited here)
| Kind | Entry | Evidence | Route |
|---|---|---|---|
| Contradicted | LOYORG-010's 3 `{HYPOTHESIS}` oracles | 200, `errors[]` absent, balance 0, ledger empty | case repair, not an oracle edit |
| Candidate new invariant | An authorization refusal must be distinguishable from an empty-scope read | Scenario 2 | `/qa-review-oracles bl` |
| `[THEORETICAL]` → context | ECL-14.3 | Lock did not refuse a live session; token minted *after* the lock still reads fine | `/qa-review-oracles ecl` |

## Risk Areas
- **Fixture cannot answer its own question.** Org pool 0 = every member 0 — equal values on both sides of the distinction under test (`test-data.md` SECOND RULE). Three charter items inherit this.
- No credit/reversal API (BL-LOY-019) means the org pool cannot be funded by any test path currently known.
- 973e7c9 ships no regression test; LOYORG-001 / -E2E-006 remain Draft.

## Questions
- Under `loyaltyMode: "Mixed Cart"`, should a points-only SKU be visible/priced while the display currency is USD?
- Is "locked membership still reads the pool as 0/empty" intended, or should it refuse?
- How is an organization's pooled balance ever credited? No seeder or API found this session.

## Charter-from-Gap
- Fund the outlet org pool, then re-run scenario 1 — the only way R5/L8 becomes decidable.
- Admin contact widget (R3/L9) for a no-org actor — not reached.
- A member holding stranded personal points **who has an org** — the branch where 973e7c9 does *not* fall back.
