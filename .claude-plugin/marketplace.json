{
  "name": "vc-tools",
  "description": "Virto Commerce internal QA tooling, published as a Claude Code marketplace for VC customers running the VC platform.",
  "version": "0.9.4",
  "owner": {
    "name": "Virto Commerce",
    "email": "support@virtocommerce.com"
  },
  "plugins": [
    {
      "name": "vc-fix",
      "source": "./plugins/vc-fix",
      "description": "Bug lifecycle for Virto Commerce QA — project setup, bug filing, autonomous fixing, fix verification, online bug monitoring, and self-diagnostics (/vc-self-check). Self-contained plugin (8 agents, 16 skills, 7 commands)."
    },
    {
      "name": "vc-perf",
      "source": "./plugins/vc-perf",
      "description": "Three-layer performance loop — BenchmarkDotNet A/B (L1), k6 load harness (L2), dotnet-trace allocation/CPU/DB attribution (L3), machine-readable perf-verdict, optimization PRs. Depends on vc-fix (reuses its onboarding, routing, and backend dev/review agents). Advisory only."
    },
    {
      "name": "vc-secrets",
      "source": "./plugins/vc-secrets",
      "description": "Secrets launcher — runs a declared process with its secrets resolved at launch, an MCP server or a task that is not one. The config entry stores a call to the launcher instead of a credential; the value comes from the OS credential store or Azure Key Vault per launch and reaches that one child only. Declarations hold references (secret:<name>) rather than values, and live in three homes (committed per repo, gitignored per repo, per user), with keystore keys namespaced per project so a project's credentials and your personal ones never share a key. Ships no MCP servers of its own. Standalone — no dependency on vc-fix."
    }
  ]
}
