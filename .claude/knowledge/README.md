# Agent Knowledge Base

Shared reference files agents consult during testing, authoring and fixing, grouped by **what an
agent reaches for the file to answer**. These are knowledge bases, not per-task notes — anything
produced by one run belongs in `reports/`, never here.

**Two indexes, and neither is hand-written.** The **Folders** table below says what each folder
answers and names the entry points a filename does not reveal. The **file-level roster** further
down is GENERATED by `npm run knowledge:index` and gated by `knowledge:index:check` — each row is
the file's own opening line, so it cannot lag the directory. This README carried no roster until
2026-09-25 because the two written here before that were stale within weeks; what was wrong with
them was that they were *typed*, not that they existed — the third, in `.claude/ROUTING.md`, was
found naming 38 files when 60 existed. Generating it keeps the CLAUDE.md rule intact: never
transcribe a roster, run the script that prints it.

The **read-before-you-write rules per file** — which file must be consulted before which action, and
the traps in each — live in [`.claude/ROUTING.md`](../ROUTING.md) §Knowledge bases. That is the part
no generator can derive, and it is deliberately NOT duplicated here.

Paths here are cited throughout `.claude/`, `ci/`, `scripts/` and the validators, so moving a file
means updating its references plus `scripts/maintenance/audit-agents-knowledge.ts` and
`scripts/maintenance/detect-vcst-isms.ts`.

## Folders

| Folder | Answers | Start at |
|--------|---------|----------|
| [`oracles/`](./oracles) | "Is this behaviour correct? Has it failed before?" — the HICCUPPS-F "Familiar Problems" set | `business-logic.md` (BL-*) · `e-commerce-edge-cases-library.md` (ECL-*) · `vc-bug-catalog.md` (VC-* historical failures) · `critical-ui-scope.md` (BL-UI matrix). **Slice, never hand over the whole file** — `npm run bl:extract` / `ecl:extract` |
| [`domain/`](./domain) | "What IS this feature and where are its surfaces?" | Two classes. **Domain maps** — slug-keyed frontmatter (`domain_slug:`), built by `/qa-domain-map <slug>`, freshness-ratcheted by `npm run domain:check`; the shape is `domain-map.md` (deliberately slug-less, so consumers skip it). **Reference files** — storefront/platform facts that are not feature-scoped (`catalog.md`, `products.md`, `store-settings.md`, `sitemap.md`, `release-ledger.md`, …) |
| [`api/`](./api) | REST + GraphQL surface, schema and authoring contracts | `graphql-schema.md` (consult before writing or reviewing any GraphQL) · `graphql-test-cases-runner.md` (the canonical runner authoring contract) · `api-auth.md` |
| [`automation/`](./automation) | How to drive the live UI as a real user | `storefront-selectors.md` · `storefront-config-flags.md` · `browser-quirks.md` (the Firefox lane prerequisites + rollback) |
| [`execution/`](./execution) | Running a flow end to end: routing, gates, data, evidence, regression mechanics, tracker ops | The single-source-of-truth files the always-loaded `.claude/rules/` tier cites instead of restating: `ticket-routing.md`, `ticket-status-transitions.md`, `quality-gates.md`, `reports-policy.md`, `test-data-authoring.md`, `when-to-write-a-test.md`, `browser-lanes.md`, plus the `regression-*.md` set moved out of `rules/regression.md` on 2026-09-08 |
| [`architecture/`](./architecture) | Repo anatomy for the developers team (auto-fix) | `vc-frontend-architecture.md` · `vc-module-architecture.md` |
| [`agents/`](./agents) | Team framework — what every agent on a team is told, before its own definition | `README.md` (roster, teams, the four-layer prompt architecture) · `qa/`, `ba/`, `developers/` `shared-instructions.md`. A plain reference dir, **not** scanned as components: agent definitions are flat files in `.claude/agents/` |
| [`ba/`](./ba) | BA documentation craft | `virto-doc-style.md` — the four audience styles (Customer / Admin / Developer / Sales) |
| [`diagnostics/`](./diagnostics) | Self-diagnostics oracle: per-skill expected phases/gates + the S0–S3 rubric `/vc-self-check` judges against | `skill-expectations.md` |

## File index

<!-- BEGIN GENERATED INDEX — npm run knowledge:index -->

_Generated by `npm run knowledge:index` — 60 files across 9 folders. **Do not hand-edit**: each
row is the file's own opening line, so a wrong row is fixed in that file, not here._

### `agents/` — 5 files

| File | What it answers |
|---|---|
| [`README.md`](./agents/README.md) | Three agent teams for the Virto Commerce platform: QA (quality assurance), BA (business analysis), and Developers (the only write-capable team — bug… |
| [`authoring-standard.md`](./agents/authoring-standard.md) | Read this before you create or restructure a component under .claude/agents/, .claude/skills/ or .claude/commands/. |
| [`ba/shared-instructions.md`](./agents/ba/shared-instructions.md) | Shared framework for the BA team — ba-system-analyzer, ba-api-specialist, ba-story-writer, and ba-doc-writer. |
| [`developers/shared-instructions.md`](./agents/developers/shared-instructions.md) | Shared framework for the developers team — one developer + one reviewer per repo kind: fullstack-backend + backend-reviewer (module/platform, .NET… |
| [`qa/shared-instructions.md`](./agents/qa/shared-instructions.md) | This file contains shared framework, classification rules, and reference patterns used by all QA interactive agents (qa-frontend-expert,… |

### `api/` — 7 files

| File | What it answers |
|---|---|
| [`api-auth.md`](./api/api-auth.md) | VC platform OAuth2 token endpoint pattern. |
| [`graphiql-interaction.md`](./api/graphiql-interaction.md) | GraphiQL uses CodeMirror — a rich text editor, NOT a standard <textarea>. |
| [`graphql-schema.md`](./api/graphql-schema.md) | GraphQL xAPI Schema Reference |
| [`graphql-test-cases-runner.md`](./api/graphql-test-cases-runner.md) | Canonical reference for writing runner-native GraphQL test cases in suite CSVs. |
| [`order-creation-matrix.md`](./api/order-creation-matrix.md) | Test matrix for end-to-end order creation covering all payment providers and shipping methods. |
| [`platform-patterns.md`](./api/platform-patterns.md) | Cross-layer architecture knowledge shared by all QA agents. |
| [`ucp-schema.md`](./api/ucp-schema.md) | UCP contract surface — GENERATED, do not edit by hand |

### `architecture/` — 2 files

| File | What it answers |
|---|---|
| [`vc-frontend-architecture.md`](./architecture/vc-frontend-architecture.md) | vc-frontend (Vue 3 / TS) storefront repo anatomy + vitest / @vue/test-utils / Storybook conventions. |
| [`vc-module-architecture.md`](./architecture/vc-module-architecture.md) | VC module repo anatomy + .NET 10 / xUnit / Angular conventions. |

### `automation/` — 3 files

| File | What it answers |
|---|---|
| [`browser-quirks.md`](./automation/browser-quirks.md) | Per-browser rendering differences. |
| [`storefront-config-flags.md`](./automation/storefront-config-flags.md) | Source of truth: vc-frontend/client-app/config/settingsdata.json (branch dev). |
| [`storefront-selectors.md`](./automation/storefront-selectors.md) | vc-frontend stable selectors (data-test-id / role / aria-label). |

### `ba/` — 1 file

| File | What it answers |
|---|---|
| [`virto-doc-style.md`](./ba/virto-doc-style.md) | Canonical style reference for BA documentation deliverables. |

### `diagnostics/` — 1 file

| File | What it answers |
|---|---|
| [`skill-expectations.md`](./diagnostics/skill-expectations.md) | The oracle for the vc-fix self-diagnostics subsystem (VCST-5476, extended by the client→vendor feedback loop VCST-5509). |

### `domain/` — 15 files

| File | What it answers |
|---|---|
| [`b2b-organizations.md`](./domain/b2b-organizations.md) | What the B2B / multi-organization feature IS — actors, value chain, the surface inventory per layer (Admin SPA / storefront / API), where the layers… |
| [`catalog.md`](./domain/catalog.md) | Platform knowledge for testing the Virto Commerce Catalog module. |
| [`configurable-products.md`](./domain/configurable-products.md) | What "configurable products" (product-configuration sections attached to a catalog product — Product / Text / File sections, each optionally… |
| [`domain-map.md`](./domain/domain-map.md) | Fill-in shape for .claude/knowledge/domain/<name>.md. |
| [`inventory.md`](./domain/inventory.md) | What "inventory & product availability" IS — the mechanism by which a product/variation becomes shown-as-available and purchasable, across Admin… |
| [`loyalty-missions.md`](./domain/loyalty-missions.md) | What the Loyalty & Missions feature IS — actors, the two independent accrual paths (LoyaltyProgramHandler earn/redeem vs LoyaltyMissionLogicService… |
| [`mobile-navigation.md`](./domain/mobile-navigation.md) | Three personas are mapped separately (§3 anonymous, §4 personal customer, §5 company member) because the drawer is not one tree with rows toggled on… |
| [`page-builder.md`](./domain/page-builder.md) | What the Page Builder / CMS-authoring domain IS and where its surfaces are — the native drag-and-drop page composer… |
| [`products.md`](./domain/products.md) | Storefront product types + xAPI fields. |
| [`release-ledger.md`](./domain/release-ledger.md) | What shipped in the Virto Commerce product line and when. |
| [`sales-rep.md`](./domain/sales-rep.md) | What the Sales Rep feature IS — a Contact + login account that holds sales-rep:access and serves a defined set of customer organizations — its… |
| [`sitemap.md`](./domain/sitemap.md) | Full storefront URL map. Customer's sitemap differs by storefront customizations. |
| [`store-settings.md`](./domain/store-settings.md) | QA testing knowledge for the Virto Commerce Store Settings module. |
| [`ucp.md`](./domain/ucp.md) | What the Universal Commerce Protocol (UCP) adapter IS — agentic commerce over MCP — from an AI agent's first tool call through to the buyer landing… |
| [`white-labeling.md`](./domain/white-labeling.md) | Platform knowledge for testing the Virto Commerce White Labeling module. |

### `execution/` — 22 files

| File | What it answers |
|---|---|
| [`browser-lanes.md`](./execution/browser-lanes.md) | MCP Servers & Browser Automation Reference |
| [`debugging-signals.md`](./execution/debugging-signals.md) | Console + network debugging heuristics. |
| [`es-call-ab-method.md`](./execution/es-call-ab-method.md) | How to answer "did this change reduce backend calls?" for an xAPI change, comparing a pre-fix deployment against a fixed one. |
| [`live-discovery.md`](./execution/live-discovery.md) | QA agents have four ways to get data into a test. |
| [`module-suite-map.md`](./execution/module-suite-map.md) | vcst's module → suite mapping. |
| [`performance-thresholds.md`](./execution/performance-thresholds.md) | Unified performance criteria for storefront, API, and admin testing. |
| [`quality-gates.md`](./execution/quality-gates.md) | This file is the only place the auto-fix gate ladder lives. |
| [`regression-lanes.md`](./execution/regression-lanes.md) | Regression — per-case lane routing, case filter, executability |
| [`regression-pipelines.md`](./execution/regression-pipelines.md) | Regression — pipelines: CI via Agent SDK, monitoring twin, staleness-audit twin |
| [`regression-promotion.md`](./execution/regression-promotion.md) | Regression — post-run promotion (Draft → Automated) |
| [`regression-scaffold.md`](./execution/regression-scaffold.md) | Regression — pre-authoring scaffold (tc:alloc / tc:scaffold) |
| [`regression-selection.md`](./execution/regression-selection.md) | Regression — change-scoped selection, existing-coverage triage, post-run triage |
| [`regression-selectors.md`](./execution/regression-selectors.md) | Regression — storefront selectors (generated, gated) |
| [`regression-suites.md`](./execution/regression-suites.md) | Regression — suite inventory, ID/naming rules, XREF-001 |
| [`reports-policy.md`](./execution/reports-policy.md) | This file is the only place the report policy lives. |
| [`test-data-authoring.md`](./execution/test-data-authoring.md) | The how of writing a test-data script for this repo. |
| [`test-execution-preflight.md`](./execution/test-execution-preflight.md) | Canonical protocol every QA agent consults before executing a test case. |
| [`test-runner-tags.md`](./execution/test-runner-tags.md) | Shared reference for test-runner-agent.md. |
| [`ticket-routing.md`](./execution/ticket-routing.md) | This file is the only place the ticket→flow routing matrix lives. |
| [`ticket-status-transitions.md`](./execution/ticket-status-transitions.md) | This file is the only place the status state machine lives. |
| [`tracker-ops.md`](./execution/tracker-ops.md) | Single reference for the interactive bug-lifecycle commands (/qa-bug, /qa-fix, /qa-verify-fix) so none of them hardcode Jira + GitHub + VirtoCommerce. |
| [`when-to-write-a-test.md`](./execution/when-to-write-a-test.md) | Repo-wide. The test-data-specific half lives in test-data-authoring.md §7a; the bug-fix half is quality-gates.md G2 and is unaffected by anything… |

### `oracles/` — 4 files

| File | What it answers |
|---|---|
| [`business-logic.md`](./oracles/business-logic.md) | Testable business rules for the Virto Commerce B2B e-commerce platform. |
| [`critical-ui-scope.md`](./oracles/critical-ui-scope.md) | Canonical scope of UI primitives that every regression run must verify. |
| [`e-commerce-edge-cases-library.md`](./oracles/e-commerce-edge-cases-library.md) | 14 generic ECL chapters (1-13, 15; universal) + 1 VC-specific chapter (14, 10 subsections). |
| [`vc-bug-catalog.md`](./oracles/vc-bug-catalog.md) | Curated reference of failure modes seen in this Virto Commerce deployment. |

<!-- END GENERATED INDEX -->

## Conventions

- **Reference, don't inline.** Agents cite an ID (`BL-AUTH-005`, `ECL-03`, `VC-CART-*`) or a path,
  never paste the body into a report. See [`.claude/rules/reports.md`](../rules/reports.md).
- **`oracles/` is the correctness backbone.** Exploratory and review flows use it as the
  "Familiar Problems" oracle and to seed Bad Neighborhood tours.
- **Generated — never hand-edit; run the script:**

  | File | Refresh / verify |
  |---|---|
  | `api/graphql-schema.md` | `npm run schema:refresh` · `schema:check` |
  | `domain/release-ledger.md` + `release-ledger-snapshot.json` | `npm run releases:refresh` · `releases:check` |
  | `domain/sitemap.md` + `sitemap-snapshot.<env>.json` | `npm run sitemap:refresh` · `sitemap:check` (diff-gated — only the sections that actually changed are rewritten) |

- **Gated — hand-written, but diffed against the source of truth:**
  `automation/storefront-selectors.md` (`npm run selectors:check`),
  `oracles/critical-ui-scope.md`'s machine-readable coverage matrix (`npm run scope:validate`),
  `oracles/business-logic.md` and `e-commerce-edge-cases-library.md` (`npm run bl:lint` / `ecl:lint`;
  ranked by `npm run oracles:rank`). A drift failure means re-grounding the file, not relaxing the gate.
- **`release-ledger.md` is the only answer to "what shipped recently"** — VirtoOZ's release corpus
  stops at Platform 3.917.1 while production is past 3.1050. Two rules travel with it: it **declares
  itself non-exhaustive**, so a miss is never proof a feature does not exist; and it says what is
  **released upstream**, never what is **deployed on the env under test** (that is a live
  `/api/platform/modules` probe — see `.claude/templates/agent-dispatch.md` §Build Verification). Its
  `stale_after_days: 45` contract is enforced by `scripts/unit/release-ledger.test.mjs`.
- **`applicability:` frontmatter** classifies each file for cross-deployment reuse —
  `universal` / `reference` / `vcst-specific`, hand-assigned in
  `scripts/maintenance/audit-agents-knowledge.ts` (which is a WRITER: it edits frontmatter in place).
- **Cross-file links use relative paths** (e.g. an `oracles/` file links a selector as
  `../automation/storefront-selectors.md`).
- **`plugins/vc-fix/knowledge/` is a separate, plugin-scoped copy**, not a symlink. Many pairs
  differ on purpose — this README among them, since the plugin ships no suites, seeders or `qa-*`
  roster. **Editing a file that exists in both trees means deciding, by hand, whether the change
  belongs in both.** There is no longer a checker: `mirror:check` was removed 2026-09-19, so drift
  between the trees is now invisible until someone reads both copies.
